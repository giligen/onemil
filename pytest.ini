[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    lifecycle: order-lifecycle tests on the in-process fake broker (always run)
    integration: tests that hit real external APIs (deselected by default; the HOD paper lifecycle needs a PK paper key)
