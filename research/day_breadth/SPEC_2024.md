# SPEC — build and score the 2024H2 holdout (PREREG_2024.md). For a Sonnet implementer, ≤ 40 tool calls

Read `research/day_breadth/PREREG_2024.md` (frozen — change nothing in it). Grep/offset reads only; never a file
> 300 lines in full. Do not commit. Do not touch `trading/`, `orb.yaml`, `config.yaml`, crontab, or any existing
cache/DB; write only under `research/day_breadth/y2024/`. No Alpaca fetch between 13:25 and 20:05 UTC.

1. `y2024/universe.py`: build the PREREG universe from `data/research/databento/equs_daily_2024H2.parquet` (columns
   bar_date, symbol, instrument_id, open, high, low, close, volume; prior close/volume via a per-symbol shift; for
   2024-07-02 the prior day is 2024-07-01) and the PIT definitions `data/research/databento/pit_definition/def_2024*.parquet`
   (raw_symbol, instrument_class). Write `y2024/candidates.csv` (symbol, bar_date). Print counts per month.
2. `y2024/fetch.py`: Alpaca SIP 1-minute bars 04:00–20:00 ET for those symbol-days into `y2024/bars.db` with the
   `bars` schema of `research/hod_pmh_causal/bars_rth.db` (copy the fetch pattern of `research/hod_pmh_causal/fetch_rth.py`:
   batched by day, resumable, retries). Then apply the 09:30–09:35 RTH volume ≥ 15,000 floor → `y2024/universe.csv`,
   and report the availability share (symbol-days with RTH bars / candidates).
3. `y2024/score.py`: import (never copy) `research/hod_consol/run_consol.py`, `research/day_breadth/breadth.py`,
   `research/day_breadth/test_1412.py`, `research/day_breadth/kept_diag.py`, `research/hod_consol/adversarial_read.py`.
   Load day bars from `y2024/bars.db` with the same RTH restriction as `run_consol.fetch_day_bars_dual` (write a thin
   loader for the one DB). Per symbol-day: find_signal → build_signal → C1 walk (as `run_consol.walk`), BR per minute over
   the 2024 universe, kept = BR(signal_m) ≥ EDGE, order_in_day among kept. Cell 1,413 = all kept; cell 1,414 = kept with
   order_in_day ≥ 4. For each: n, net mean, t (adversarial_read.stats), rest mean (1,413), placebo_riskon mean and
   setup − placebo, weekly R series → weekly Sharpe and green weeks, fills/week, the slotted (simulate_slots) book,
   worst day, worst week, top-10 %-days share, and the PREREG pass verdict. Write `y2024/REPORT_2024.md` and the
   trade CSVs.
4. Tests: `tests/test_y2024_universe.py` — the universe filter on a synthetic daily frame (gap boundary 3 %, price
   bounds, prior-volume floor, class filter, test-ticker exclusion). Keep the full suite green.

Reply ≤ 150 words: universe size, availability share, then per cell n / net R / t / setup − placebo / weekly Sharpe /
green weeks / verdict.
