# FRAMES.md — the standing frame ledger for HOD-break

One line per FRAME ever tried. A *frame* is a way of extracting money from the book — not a cell, not
a knob. 799 cells were spent inside frame F1 before anyone wrote this file down; the point of the
ledger is that the next pass can see what has already been framed and what has not.

Owner 2026-09-19: *"continue and iterate and look at it from scratch every time, with fresh eyes,
using different ways to extract $$$ from this. You won't stop till this happens."*

| # | frame | mechanism | cells | date | result |
|---|---|---|---|---|---|
| **F1** | **Filter the signal minute, shape the exit** — the whole programme through 2026-09-19 (`hod_break` 12 causal cells + `hod_filter_stack` 580 + `hod_preopen_regime` 162 + `hod_losers` 20 + `hod_fresh` 15 + the entry-cost audit) | a causal feature known at the break bar separates good breaks from bad ones; or a better exit keeps more of the good ones | **799** | 2026-09-13 → 09-19 | **DEAD.** 0 of 799 clear the claim bar; 0 clear the live-exploration bar. Best ever: `hod_fresh` C1 (old-HOD × last-5-bar stop × SPY-up) TRAIN +0.100 gross / +0.033 net / +$2,391, VAL +0.123 / +0.050 / +$1,827 at 13.8–16.0 tr/wk, same-signed positive in H1/H2/VAL — the only such cell in 799 — but clustered t +0.54 / +0.66, green weeks BELOW its own null mean on both splits, one week carrying 85 % of the TRAIN year, and net negative ex-top-5 %. Best gross separator in the programme is a NON-causal one (`MFE@10min`, 0.77–0.88 R) worth **+0.012 R** as a rule. Standing findings: ORB's Rule M is REVERSED here, Rule D is structurally inert, the breakeven move is the worst exit in the set, `touch_n >= 5` and `consol_bars >= 8` are the WRONG side as first-break filters, the booked-set cost is **0.061–0.065 R** (not 0.2151), and the +0.43 R / −0.55 R two-cohort split of `bf_zero` §6b is the one unexploited structure F1 never addressed. |
| **F2** | **SHORT the failed break** | the loser's path (+0.45 R at minute 6 → −1.06 R at minute 28) IS a trade on the other side; the long book books the bleed by minute 10 and cannot monetise it, a short entered at the confirmation can | 26 | 2026-09-19 | **DEAD.** 0 of 26 clear either bar. Pure short book (whole B2 population, `fb` trigger) −0.080…+0.032 gross / −0.164…−0.040 net at 7.7–26.0 tr/wk; no cell same-signed positive across H1/H2/VAL at ≥10 tr/wk; the SPY-down mirror does not save it; the **borrowable subset is the WORSE half** (VAL −0.131…−0.151 gross). Only same-signed-positive cells (`a01`/`a00`/`fb` × mfe × the long's stop level, on booked longs) run at **1.8–4.0 tr/wk** with negative VAL dollars. **Borrow is NOT the constraint: 62.3 % of short signals are `shortable AND easy_to_borrow`** (halt-resume 8.1 %); the edge is. The no-chase cap is **protective** on this side (obtainable −0.168/−0.666 R vs un-obtainable −0.023/−0.019) — the halt-resume dip-buy signature in a new book. Short R is 0.56× the long R, so the same spread costs ~1.8× more per R; short-entry NBBO is 99–100 % imputed (flagged). |
| **F3** | **The noon conditional-mover book** | `bf_zero` §6b: EOD range >= 10 % days are +0.43 R and the rest −0.55 R in EVERY split; EOD range is unobservable at 10:00, but range is monotone within the day, so "range so far >= X % at T" is a causal proxy for the cohort | 9 | 2026-09-19 | **DEAD, and it CLOSES the two-cohort structure.** The proxy works spectacularly — P(EOD range ≥10 % \| range ≥8 % at 11:00) = **96.2 %** (79–96 % across all nine cells, base rate 62–68 %) — and the book does not: 9 cells at **0.9–6.6 tr/wk TRAIN**, best +0.141 gross / +$1,496 TRAIN with **negative VAL dollars**, both same-signed-positive cells inside their own nulls. **§2.3 is the finding**: the oracle cohort on this book is +0.266/+0.360 gross, 71.7 %/82.6 % green, +$20,585/+$13,754 — and a causal proxy reaching it at **97.3 %/98.8 % accuracy** captures **+0.102/−0.076 R** of it. Inside the cohort, days **already** wide at 11:00 read +0.118 R at **40.9 % WR**; days whose range **arrived after the signal** read +0.882 R at **82.0 % WR**; `corr(gross R, range added after the mark)` = **+0.448/+0.181**. **The ≥10 %-range-day marker is downstream of the trade's own outcome.** `bf_zero` §6b stands as a description of two populations and is NOT an admission rule. |
| **F4** | **Size by regime, frequency preserved** | every gate in 799 cells threw picks away and lost week shape to arithmetic; a sizing rule keeps all ~30 picks/week and moves only dollars. `spy_r5_pct` (SPY 09:30→09:35, known 09:35:00) separates the book +0.388 / +0.189 R | 4 | 2026-09-19 | **DEAD — and the cleanest demonstration that the owner's primary metric moves without any edge.** Pick set identical to the shipped book (gross R/trade unchanged to the 4th decimal, the declared check). All four sizers lift VAL green weeks **43.5 → 65.2 %** and VAL dollars **+$893 → +$3,886 / +$5,605 / +$6,878 / +$12,821**, three of them above their key-shuffled null bands on VAL — and **all four leave TRAIN negative** (−$6,118 … −$13,261). A sizing rule re-weights an edge; it cannot create one, and re-weighting a losing year gives a smaller losing year. S4 (keyed on Frame 2's range proxy) looks best and is the same leakage illusion measured in dollars. |

*(F2/F3/F4 above are this pass's Frames 1/2/3; they take the next free ledger numbers.)*

---

## THE QUEUE — frames declared and not yet run (owner 2026-09-19, appended before this pass scored)

Run one heavy job at a time. Each is a FRAME, not a cell: it changes what the book *is*, not a knob
inside it.

| # | frame | mechanism |
|---|---|---|
| **F5** | **The RETEST book** | admission = the **SECOND** HOD break of the day only, after a first break that failed or retraced. The first break's failure IS the filter (it flushes the impatient longs and the stops above the level), and the retest confirms the level held. A new admission rule, not a feature on the existing one — which is why 799 feature cells could never have found it. |
| **F6** | **ABSORPTION at the level** | volume traded within ±0.5 % of the HOD in the bars that FORMED it, as a share of ADV. A heavy shelf at the high = sellers absorbed = supply cleared before the break. This is the plausible mechanism behind the one admission that held on all three eras (`hod_fresh`'s `consol_bars >= 20` control) and it has never been measured directly. Ladder it. |
| **F7** | **entry-minute × SPY-state 2D map** | the two strongest single features in the programme (entry minute, +0.518 separation; D2 `spy_r5_pct`, +0.220) have **never been crossed**. A decile × {SPY up, SPY down} heat map of R, then the best 2–3 cells promoted to rules with the multiplicity of the whole map accounted for. |
| **F8** | **INSTITUTIONAL FOOTPRINT** | cumulative $ volume from 09:30 to the signal minute as a share of 20-day average daily $ volume — distinct from `bar_vol_x`, which was the breakout bar alone, and from `rv_profile`, which is share-volume against a clock. >= 50 % of ADV$ done by 10:00 = the name is being bought by size, not by the tape. Ladder it. |
| **F9** | **The signal-minute cohort field** (NEW, written by this pass) | F3 proved a **+0.8 R** separation exists between days that keep expanding and days that do not, and that every proxy for it arrives too late (11:00, when only 23.9/17.4 signals a week remain). The frame is to find a field computable **at the break bar** that ranks "this day will keep expanding" WITHOUT being downstream of the trade: cumulative $ volume vs ADV$ at the signal (this is F8, and it should be run INSIDE F9, not separately), the count of prior 5-minute range expansions in the session, and the ratio of range added in the last 30 minutes to range added in the first 30. **Mandatory gate on every candidate before it is scored as a rule**: the already-there / arrived-after split of `hod_frames/REPORT.md` §2.3 — a field that only marks days whose range arrived after the signal is the trade's own outcome wearing a hat. `dist_open_pct` is already refuted as the obvious version (ranks membership Q1 40.5 % → Q5 85.0 % and is flat on gross in both years). |

**Recommended run order after this pass: F5, then F6, then F9 (absorbing F8). F7 stays queued.**

---

## PASS 2 RESULTS — F5, F6, F9 RUN (`research/mature_method/hod_frames2/`, 2026-09-19, PREREG `1f26c7a`)

| # | frame | cells | result |
|---|---|---|---|
| **F5** | **The RETEST book** | 7 | **DEAD — and the lead that ranked it first was a different object.** `hod_losers` §6's "+0.113 / +0.236 R for re-breaks" is reproduced exactly (**+0.236 TRAIN / +0.066 VAL**) and it compares **175 / 69 first-qualifying signals whose symbol-day had an earlier candidate break that did not QUALIFY** — one trade per symbol-day in both arms, never a second trade. Built as a genuine second trade on a new all-breaks walk (**1,188,186 qualifying break rows**, 98,203 symbol-days): the retest is **−0.027 / +0.000 R at the signal level** and **−0.066 / −0.000 gross, −0.147 / −0.085 net** as a book at 11.9 / 17.5 tr/wk. **The failure filter is inert** — "the first break failed on price" selects **783 of 783** second breaks; the discriminating version (the predecessor's trade actually stopped, causally, before our bar) leaves **2.0 / 3.7 tr/wk** at +0.154 / +0.014 gross with negative VAL dollars and MDE 0.38 / 0.40 R. The 3rd+ control is the only same-signed-positive admission (+0.012 / +0.018 / +0.080) and is a **slot artifact**: at the signal level it is +0.016 / **−0.030**, i.e. the 12/4 first-come rule picking out of 481 signals a week, at 96-97 % imputed cost, net-negative on both splits. `corr(gross R, range added after)` on every retest cell is **+0.31 … +0.65** — the retest inherits §2.3, it does not escape it. |
| **F6** | **ABSORPTION at the level** | 9 | **DEAD, and it REFUTES the mechanism story for `hod_fresh`'s control.** The shelf (volume inside HOD ±0.5 % before the break, as % of ADV — median 1.9 % over 2 bars) is non-monotone and sign-flips at every rung above 2 %: `>=20 %` is **+0.211 TRAIN / −0.180 VAL** on 72 / 39 trades. Shelf **DURATION is the wrong side on BOTH splits** (`shelf_bars >= 5`: −0.065 / **−0.175**, iid t −2.10) for a legible reason — a long shelf marks a **quiet** day: the ≥10 %-range-day share falls from the base's 59 / 60 % to **44 / 38 %**, while shelf VOLUME raises it to 79 / 77 %. `hod_age_bars >= 20` (the age arm) is negative on both splits (−0.086 / −0.111). **`consol_bars >= 20` is neither absorption nor the age of the high** — it encodes *the low staying up*, a base that holds, not supply that clears. The only admission in 896 cells same-signed positive in H1/H2/VAL is now formally **unexplained**. |
| **F9** | **The signal-minute cohort field** (absorbing F8) | 10 (3 excluded, 2 void) | **DEAD, and it CONFIRMS §2.3 by a second route.** `rng_own` (session range so far ÷ the symbol's own 20-day median daily range) at `>= 1.5` puts **96.8 % / 95.7 %** of its trades on ≥10 %-range days — pass 1's 11:00 proxy accuracy, now **at the break bar, on every signal, at every hour** — and its gross is **+0.038 / −0.097** against a base of −0.002 / +0.015. **Membership is solved and carries no return.** The two biggest |t| in the pass both sign-flip: `exp5_n >= 6` **TRAIN +0.232 at t +4.96 / VAL −0.145 at t −2.11**; `add30_ratio >= 1.0` **TRAIN +0.570 at t +10.14** (the largest t in the programme) **/ VAL −0.094** — and `supp2.log` S7 shows it is the **entry clock**: the field is undefined before 10:30 and the late cohort alone is +0.319 TRAIN / −0.025 VAL. The availability rail (58.8 % vs 73.2 % missingness on winners vs losers) **voided it before the number was looked at**. **`dollar_frac` (= F8, cum $ volume ÷ 20-day ADV$) was EXCLUDED by this pass's own pre-committed downstream gate and NOT scored** — and at its top rung it is the only field in the pass with a same-signed gross separation on both splits (+0.076 / +0.071, t +1.28 / +1.00). It is quarantined, not refuted; it goes to F10. |

**Pass 2 rails**: reproduction EXACT (`B2` −$17,346 / +$893, Δ$ 0); **independent rebuild EXACT** (a new all-breaks loop vs `pass2.py`: 98,203 shared symbol-days, same entry minute **100.0000 %**, max |Δstop| 0.00, max |Δrr| **3.55e-15**); 44 of 44 nulls INSIDE their band (0 above, 0 below); cost re-measured per cell (0.044-0.086 R); TEST never opened. **Programme cell count 870 + 26 = 896.**

---

## THE QUEUE — rewritten by pass 2 (F7 is absorbed by F12; F8 is absorbed by F10)

| # | frame | mechanism |
|---|---|---|
| **F10** | **The quarantined footprint field, with the gate re-specified** | `dollar_frac` — cumulative $ volume 09:30→signal as a share of 20-day ADV$ — is the ONLY field in 896 cells whose top rung separates gross with the **same sign on both splits** (+0.076 / +0.071), and pass 2's own pre-committed gate excluded it before scoring. **The gate as written is over-broad and that is the finding**: being computed from post-signal data is what makes a field downstream; *marking days that are already wide* is observable at the signal and is a statement about WHICH HALF of the cohort a field selects, not a causality violation. F10 re-specifies the already-there / arrived-after split as a **reported diagnostic**, not an exclusion, and pre-registers: decile ladder on `dollar_frac`, the era-consistency requirement (same sign in H1, H2 and VAL) as the ONLY selector, the cross with the shipped stack, and a dedicated NBBO fetch at the selected rung's own minutes (its cost is 20-40 % imputed). Mechanism: half a day's average dollar volume done before 10:30 is size participating, not the tape churning — the one thing in this programme that is about WHO is buying rather than about the shape of the bars. |
| **F11** | **Identify `consol_bars`, the last unexplained survivor** | `consol_bars >= 20` (`hod_fresh` C1) is the only admission in 896 cells same-signed positive in H1, H2 and VAL, and F6 has now refuted both natural mechanism stories for it — shelf volume at the level and the age of the high are each NEGATIVE. An admission whose mechanism is unknown cannot be trusted at 13.8/wk and cannot be extended or ported to another book. The frame decomposes the field into its candidate contents and scores each SEPARATELY on the same cascade: (a) the rising-lows sequence (higher lows into the break, which is what "every low within 4 % of the level for 20 bars" mostly is), (b) range compression into the break (ATR14 falling, the classic coil), (c) the entry minute it implies (20 bars of holding cannot happen before 09:56 — the clock confound that killed `add30_ratio`), (d) the price band and the spread it selects. If (c) or (d) carries the sign, C1 is a clock or a liquidity cell and the programme's one survivor is retired; if (a) or (b) does, there is a mechanism to build on. |
| **F12** | **ADAPTIVE vs FROZEN — refit the admission on a rolling window** (absorbs F7) | Every strong separation this programme has found changes sign between 2025 and 2026: entry minute (+0.518 the strongest single feature, and the direction flips), `exp5_n` (+0.232 → −0.145), `add30_ratio` (t +10.14 → −0.094), `rv >= 5` (+0.177 in `hod_break` → negative in `hod_fresh`), the SPY 09:35 gate (+0.725 H1 → −0.041 H2 on the base). **Nine passes have asked "which frozen rule is right" and the data keeps answering "it depends on the regime".** ORB settled the identical question on 2026-09-08 with the honest dump: refitting the SELECTION weekly on a rolling 26-week window beats frozen ($7,588 vs $5,669, MDD −223 vs −551, robust for windows ≥ 20 weeks) while refitting the SIZING is the whipsaw. HOD-break has **never** been tested with a rolling refit of anything. The frame: walk forward week by week, refit ONLY the admission window (the hour band, plus one ranked feature cut) on the trailing 26 weeks, book the next week out of sample, and compare against the frozen book on the owner's metric — with the pre-committed rails that the refit may never see the week it trades, that the window length is declared in advance (26 weeks, with 20 and 34 as robustness arms), and that a refit book must beat frozen on BOTH TRAIN halves and VAL to count. This also subsumes the queued F7 (entry-minute × SPY-state), which is one frozen cell of the same map. |

**Recommended run order: F11 first** (it is cheap — the artifacts exist — and it can retire or explain the programme's only survivor), **then F10** (one NBBO fetch, then a ladder), **then F12** (the heavy one; it changes what a rule IS).

---

## PASS 3 RESULTS — F11, F10, F12 RUN (`research/mature_method/hod_frames3/`, 2026-09-19, PREREG `b4b8171`)

| # | frame | cells | result |
|---|---|---|---|
| **F11** | **Identify `consol_bars >= 20`** | 10 | **DEAD, and the programme's only unexplained survivor is RETIRED.** None of the four candidate contents reproduces its signature on the same cascade: rising lows (`hl_n20 >= 12/16`, `lo_slope20 > 0`), range compression (`atr_ratio <= 0.8/0.6`), the clock it implies (`break_m >= 590/600`) and the price/liquidity band it selects (`price >= $37.4`, `spread/R <= 0.0932`) — **0 of 10 same-signed positive in H1/H2/VAL**, so the conditional SPY cross was not scored, as declared. The pre-committed rule returns UNRESOLVED and its declared tie-breaker settles it: inside the clock admission the field is worth **+0.018 R TRAIN (clustered t +0.09) / −0.133 R VAL (clustered t −1.84)**, and **on C1's own SPY-up days it is −0.135 / −0.188, the WRONG side on both splits**. The three-era sign belongs to the **SPY 09:35 day gate**: `base × spy_r5 > 0` (no `consol_bars` at all) is **+0.032 / +0.165 gross, 43.4 / 65.2 % green, −$3,059 / +$3,439** at 15.6 / 15.9 tr/wk — a better VAL cell than C1 — and `break_m >= 590 × spy_r5 > 0` is **same-signed positive in H1/H2/VAL** (+0.094/+0.008/+0.112) without the field. The field **dilutes** the gate (separation +0.388/+0.189 on the base vs +0.219/+0.147 on the ge20 admission) and mechanically just **waits ~37 minutes** (it takes a later break 27/33 % of the time; those later rows are the TRAIN loss, −$3,591). As a continuous ranker it is non-monotone and flips (VAL Q5 −0.109). **920 cells, and the programme has no unexplained survivor left — it has a day gate whose own book is H1-negative.** |
| **F10** | **The footprint field, gate re-specified** | 8 declared, 5 scored | **DEAD — and the withdrawal of pass 2's exclusion changed nothing.** With the already-there / arrived-after split demoted to a reported diagnostic, `dollar_frac`'s decile ladder is **H1-2025-negative at p50/p60/p70/p80**; the only era-consistent rung is **p90 at 5.7 / 8.0 trades a week** (MDE 0.216 / 0.274 R) — under the declared floor, the seventh survivor in this programme to land there. The **dedicated NBBO fetch** (466 fresh Alpaca-SIP quote-minutes at the p80 rung's own signal minutes) cut its imputed-cost share **44.6 % → 3 / 5 %** and lifted its gross to **+0.065 / +0.096** with net **+0.009 / +0.034** and **60.9 % VAL green weeks** — **and its H1 stayed −0.024. Cost was never the reason.** The §2.3 diagnostic is *inconsistent between the years at every rung* (TRAIN's gross runs through the arrived-after channel, VAL's through the already-there one), which is the honest content of the pass-2 quarantine. F10-6/7/8 (the crosses with the SPY gate, with `consol_bars >= 20`, with C1) were NOT scored, as declared. |
| **F12** | **ADAPTIVE vs FROZEN** | 6 | **READING (ii): the separations are NOISE, and every future frozen-rule pass on this book is PRE-REFUTED.** Walking forward week by week and refitting ONLY the admission window (hour band + one ranked cut from `rv_profile`/`dollar_frac`/`consol_bars`/`spy_r5`) on trailing 20/26/34 weeks, booking each next week strictly out of sample (leakage asserted in code — the assert fired once and caught a real defect, the shared 2025-12-27 week), the refit **loses to frozen on green weeks in every arm and every OOS half**: 36.4 / 30.6 / 31.7 % vs **38.2 / 38.8 / 39.0 %** over 55 / 49 / 41 OOS weeks, at a third of the frequency (9.5–11.4 vs 31.7 tr/wk), clustered t −0.64 / −1.32 / −0.69. **Churn 83 / 81 / 65 %** (44 distinct configs in 55 weeks) and the band **alternates between the earliest and the latest entry-minute deciles** — the two opposite ends of one axis. The chosen CUT is stable in kind (`spy_r5` wins 28/55, 21/49, 26/41 weeks — the same day gate F11 identified). ORB's 2026-09-08 answer does **not** transfer: ORB refits the ranking of a selection stack that has an edge to rank. The **sizing control** behaves exactly as Frame 4 predicted — identical picks, L=34 reads 46.3 % green and **+$3,441** vs frozen's 39.0 % and +$163 — and is reported, not recommended, because a sizer re-weights an edge and cannot create one. |

**Pass 3 rails**: reproduction EXACT on FOUR rows (`B2` −$17,346 / +$893 **and** `hod_fresh`'s `consol_bars >= 20` rung and C1, reproduced from a different population object — the all-breaks stream rather than that pass's own per-rung walk); availability audit passed by all four new fields (gaps 1.3–2.8 pp); cost re-measured per cell (0.047–0.092 R) plus a dedicated fetch; **40 cell × split nulls: 34 inside, 5 below, 1 ABOVE** (and the ABOVE is a cell that loses money on both splits — F7's lesson, third appearance); TEST never opened. **Programme cell count 896 + 24 = 920.**

---

## THE QUEUE — rewritten by pass 3 (F12 closes the frozen-rule line; what is left is not a rule on the bar)

Pass 3 removed the two things the queue was built on: the survivor is retired and the "it's the regime"
alternative is refuted. **Nine passes and 920 cells have all varied the same two objects — which break
is ADMITTED and how it is EXITED.** The three frames below are the objects that have never been varied.

| # | frame | mechanism |
|---|---|---|
| **F13** | **The SLOT RULE: rank, don't race** | Every one of 920 cells changed the admission or the exit; **not one changed how the book chooses among simultaneous candidates.** The 12/day × 4-concurrent rule is **first-come by entry minute, ties by symbol** — and `hod_fresh` §3 measured what that costs: the fresh-high rungs' signal-level gross **+0.064 / +0.040** becomes **+0.007 / +0.055** once the slot rule has taken the earliest rather than the best (1,850 signals → 809 fills). The pre-book stream is ~94 signals a week and the book takes 30. The frame: replace the race with a **causal ranking**, exactly as ORB ranks its 09:35 cohort — accumulate the day's candidates to a declared decision window (every break so far at T ∈ {10:00, 10:30, 11:00}, or a rolling K-minute queue), rank by a declared causal score, fill 4 slots, and re-run the whole frontier. Mechanism: the slot rule is a **sampling rule**, and a sampling rule that correlates with the entry minute is sampling the programme's most sign-unstable feature. This is implementable live (the engine already streams the whole universe and holds a candidate set) and it is the one lever that can raise the book's gross **without** needing a new separator — it only needs the existing ordering to be non-random. Pre-register the ranking score from fields already measured, and report the ceiling: the same book ranked by a PERFECT oracle score, which bounds what any ranking can be worth. |
| **F14** | **The DAY is the unit** | After F11, the only object in this programme that is same-signed positive across three eras is a **day gate** — `spy_r5 > 0`, known at 09:35:00 — and every measurement of it has been made with **trade-level** power on a day-level variable (344 days, not 7,027 trades; `hod_preopen_regime` §4's own clustering correction is the tell). The frame: make the DAY the observation. Score `Σ R` per day, ladder the 09:35 realised tape — SPY/QQQ/IWM five-minute return **and its magnitude, not only its sign**, the SPY 09:30–09:35 range, breadth of the book's own candidate set (how many names broke by 09:35), the VIX open gap — with day-clustered inference that is now the *unit* rather than a correction, and ask the one question the trade-level view cannot answer: **is there any day-level state under which the book's own H1-2025 is positive?** The whole programme's failure is H1-shaped (base H1 −0.077, the gate's own cell H1 −0.062) and no pass has ever conditioned on the day as an observation. Mechanism: a long-only continuation book is a levered bet on intraday drift; whether a breakout continues is a property of the tape, not of the bar that broke. |
| **F15** | **WHO is breaking out — the instrument, not the bar** | 920 cells describe the **shape of the bars**. Not one conditions on **what the name is**. ORB's own rule book found the instrument load-bearing (`research/orb_machine_rules.md`: 45 % of its universe are leveraged wrappers with no company events, and the news boost had to be gated on positive identification as common stock — `trading/orb_asset_class.py`, a 33K offline map that already exists). The frame: ladder the book on **causal, static-at-the-open instrument attributes** — wrapper vs common (`orb_asset_class`), float and shares outstanding, prior-day short interest and days-to-cover, listing venue, sector, ADV$ band, and whether the name carried premarket news (`data/research/orb_news_catalyst_*.csv`, already built and already causal at 09:31) — each with the era-consistency requirement and the frequency floor. Mechanism: continuation past a high-of-day requires **forced** buying; forced buying is a float / short-interest / index-membership property of the instrument, not of the last twenty one-minute bars. This also gives the first honest answer to a question the programme has never asked: is the HOD-break edgeless *everywhere*, or edgeless *on average over a universe that mixes 2× wrappers, $40 large caps and microcaps into one book*? |

**Recommended run order: F13 first** (it is the cheapest — no new bar walk, `breaks2.csv` already holds every qualifying break of every symbol-day, and the oracle bound tells you in one cell whether the frame can matter at all), **then F14** (a day-level rebuild of the one surviving object, ~344 observations, cheap), **then F15** (needs a float / short-interest join; the wrapper and news joins already exist).
