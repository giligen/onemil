# TEST SEAL — frames7

TEST is **2026-06-01 onward** for all three books (ORB G3, BF P1, HOD-break B2) and is **not opened**
in this pass. `c7.split_of` returns `'TEST'` and every scored frame filters to `('TRAIN','VAL')`.

Unsealing requires: a committed recommendation, the owner's word, and a fresh pre-registration that
names the single number TEST is being asked for.
