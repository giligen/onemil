# TEST SEAL — frames8

TEST is **2026-06-01 onward** for every book touched in this pass (HOD-break B2, ORB) and is **not
opened**. `split_of` returns `'TEST'` and every scored cell filters to `('TRAIN','VAL')`.

Unsealing requires: a committed recommendation, the owner's word, and a fresh pre-registration that
names the single number TEST is being asked for.
