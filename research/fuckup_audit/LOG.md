# fuckup_audit — daily log (three lines per stage; answer "status" from here)

## 2026-09-16 — Stage A (A0 cost contract, A1 re-gate, A2 time bands, A3 intraday state)
- RAN: `A/extract_pop_a.py` (446,012 rows = score4's population exactly), `A/a0_score.py` (26 keys x 2 exits x 3 splits under 6 cost contracts, booked with run_book(12,4); contract (b) reproduces `score4_results.csv` cell for cell, max |dn|=0, max |d meanR|=0.000), `A/a1_gate.py`, `A/a2_bands.py`, `A/a3_state.py`, `A/a4_decomp.py` -> `A/REPORT.md`.
- SAYS: the corrected cost contract is worth **+0.412 R/trade** on average (spread table +0.357, entry coefficient 1.0->0.25 +0.066, charging the target exit -0.010) and flips **9 of 52 TRAIN cells positive** (score4: 0 of 52) — but **0 of 52 clear G1** (best t 1.56 vs the ~2.7 that 52 nulls would give); the only G1 passes in the stage are F6 hold/2r and F1 P=0.12 hold restricted to entries >= 10:30 (t 2.08 / 2.25 / 2.01), and all three fail G2 on VAL (t 0.49 / 0.53 / -0.38), so TEST was not read for selection. Intraday market state at the entry minute: TRAIN->VAL sign agreement 0.51 over 104 buckets — a coin flip. Smallest per-trade effect the headline tests could see: 0.066 R (F8 N=30), 0.106 R (F6), 0.476 R (F1 P=0.12).
- NEXT: Stage B — rebuild pass 1 as `candidates4.csv` carrying BOTH fills (next-open and resting, H2), signal-bar OHLCV, level history and MAE/MFE, with the H1 stop x exit matrix computed in the walk; score it under the corrected contract (c) adopted here, base families F6 / F8 N=30 / F8 N=15, entries >= 10:00, with the intraday index sign kept as a Stage-D feature and not as a filter.

## 2026-09-16 — Stage B (pass-1 rebuild: both fills, level history, MAE/MFE, H1 stop matrix, H9 families)
- RAN: `B/build_candidates4.py` (14 family-configs incl. the four NEW H9 shapes F11 close-confirm / F12 retest / F13 sweep-and-reclaim / F14 second break; one row per SIGNAL, not per fill; `entry_next` AND `entry_rest`; signal-bar OHLCV; n_touches / consol_bars / consol_vol_ratio / cum_dollar_vol / vwap_dist / pm_dollar_vol / prev_day_range / asset_class; MAE/MFE; the H1 stop variants close-stop and stop-1% and the ORB lock, each with its own exit minute) -> `B/candidates4.csv`, launched detached at ulimit 3.5 GB / nice 10, ETA ~85-105 min; plus `B/score5.py` (the Stage C scorer, written and smoke-tested, NOT run for results) and `B/REPORT.md` (the pre-registration).
- SAYS: **parity exact** — on two 3-day windows (2025-01-02..06 and 2025-10-21..23) all 7,052 / 6,178 `candidates3` rows of the shared families match `candidates4`'s next-open fill to **0.0** on entry and rr_2r, with 0 entry/exit-minute and 0 `why` mismatches; the same days carry 2,721 / 2,216 EXTRA signal rows candidates3 could never hold (86% / 69% with no next-open fill) — the selection that made H2 unanswerable. Two facts pinned before Stage C: **F12's base stop is structurally < 1% of price** (retest band 0.3%), so under PLAN §1's R floor it is scoreable only via the `2R stop-1%` variant; and the resting fill is **100% obtainable but only 56-88% queue-OK** (5x-volume check), so H2's ">= 95%" rule will be decided on the queue-restricted set. Cells pre-registered for Stage C: **115** (14 configs x 2 fills x 5 outcomes, minus 25 impossible resting cells for the close-triggered families), +115 for the separately counted `entry_m >= 600` pass.
- DONE 22:37 UTC, `EXIT=0`: 420/420 days, **3,099,499 signal rows, 1.99 GB, 138.5 min**. Verified four ways — day count 420, row count matches the log's running total, header IDENTICAL to the pinned 77-column `COLS`, **FULL 420-day parity** (913,985 candidates3 rows of the shared families, 0 unmatched, `entry` and `rr_2r` max abs diff **0.0**, 0 minute/reason mismatches, +290,719 signal rows candidates3 could never hold, 77.5% of them with no next-open fill), and `verify_rows.py 5` hand-recomputing five random rows from the raw bars with a plain loop — ALL MATCH. **H2 half-settled: the resting fill is obtainable on 100.000% of fills on every family and lifts the fill rate by +0.10 to +0.32 (F1 0.531->0.846, F6 0.680->0.820, F5 0.815->0.948) — but the 5x-volume queue check passes on only 0.57-0.88, far under the PLAN's 95%, so every resting cell must be reported unrestricted AND restricted to `rest_queue_ok == 1`.**
- NEXT: when `B/build4.log` ends in `DONE` + `EXIT=0`, verify row count / column alignment / re-run `B/parity_smoke.py` on the finished file / hand-recompute 5 random rows, then Stage C = `B/score5.py` (and `--min-entry-m 600`, `--free-target`, `--legacy-spread` as declared sensitivities). TEST stays unread until a G2 survivor exists and the selection is frozen in writing (`SCORE5_READ_TEST=1`).

## 2026-09-16 — Stage D0 (H4 feature-selection PILOT, on the features already on disk)
- RAN: `D/d0_news.py` (fresh causal Alpaca news pull, 420/420 days, **61,902 symbol-days, 100% fetch_ok, 17.8% with premarket news**), `D/d0_table.py` (90,376 candidates = F6 {} / F8 N=30 / F8 N=15, score4 population, entries >= 10:00, exit `hold` pre-registered per family), `D/d0_features.py` (33 features, **all causal at the SIGNAL minute** — the entry-derived `price` / `r_pct` / `dist_open_pct` of `candidates3` are excluded and causal twins rebuilt from `level`/`stop`), `D/d0_parity.py` (**6/6 vs Stage A's `a2_cells.csv`, max |dn| 0, max |d meanR| 0.0004**), `D/d0_model.py` (HGB regressor + classifier, one fixed config, monthly walk-forward refits 2025-10 -> 2026-09, on three tapes: real / Nagel-reversed / shuffled; plus the frozen transparent decile baseline), `D/d0_eval.py`, `D/d0_perm.py`, `D/d0_tables.py` -> `D/PREREG.md` + `D/REPORT.md`.
- SAYS: **a feature model does not select a positive book out of these families.** Selection beats the unselected first-come book by **+0.020 / +0.004 / -0.021 R per trade** on TRAINPRED / VAL / TEST. **G1: 0 of 18** cells (best t 1.16). **G2: 5 of 18** clear the arithmetic on VAL (best `F8 N=15` reg S2 +0.151 R, t 2.42, 64% weeks green) — but **the transparent baseline clears G2 in 0 of 6**, which PLAN §3 H4 makes a hard ship requirement, so **no ship candidate exists**; **2 of the 5 FAIL the Nagel reversed-tape gate on VAL** (+0.065 t 1.42 and +0.090 t 2.22 with every target's sign flipped), **all 5 go negative with the top 5% of trades removed**, and after the rule was frozen in writing **TEST returned all five negative** (-0.020 to -0.138 R; only 5 of 18 cells positive on TEST vs 16 of 18 on VAL). On TEST the reversed tape is positive for EVERY F8 cell, up to **+0.187 R, t 4.39, 14/14 weeks green** — the ranking is inverted out of sample, the Nagel signature exactly. Decile-calibration rho flips sign between consecutive out-of-sample periods for 7 of 9 family x model pairs. Search-adjusted permutation p on VAL 0.034 (conditional on the fits, therefore understated; the one full-pipeline shuffled draw reached 65% of the observed statistic). News presence alone is inert here (permutation importance ~0, mean rank 16-19 of 33) — but the live ORB rule is `PM$ > cut AND has_news`, and PM$ does not exist in `candidates3`, so only one leg of a two-leg rule was tested. Smallest per-trade effect visible on VAL: **0.088-0.209 R** (~1.7-3.2 R/week at 4 slots); the +0.2..+0.4 R that the BF/ORB selection stacks historically carried IS excluded at this power.
- NEXT: **D1 — re-run the identical pipeline on `B/candidates4.csv`** the moment Stage C is done (`d0_table -> d0_features -> d0_parity -> d0_model -> d0_eval trainpred val -> d0_perm -> freeze -> d0_eval test`, nothing else changes). What D1 adds and D0 could not have: the **resting fill** (+30% signals; `candidates3` is conditioned on cheap fills, so D0's population is missing the fastest continuations by construction), the signal bar's OHLCV and the level's pre-signal history, **premarket dollar volume** (the missing leg of the ORB news rule), prev-day range % and range-size %. Standing recommendation from this stage: **promote the reversed-tape twin to a requirement for every model-based claim in this program** — it flagged the two worst cells before TEST was opened.

## 2026-09-16 — Stage D1 input: premarket dollar volume backfill (the missing leg of the ORB news rule)
- RAN: `D/pm_backfill.py` (detached, ulimit -v 1 GB, nice 10, 9.4 min, EXIT=0) over the 61,902 (day, symbol) keys of `D/table.csv` (F6/F8, entry_m >= 600) — 04:00-09:29 ET aggregates from the 1-min SIP tape, DST-exact per-day boundaries via zoneinfo; keys already carrying a pre-09:30 bar in `research/bf_zero/bars_sip.db` computed from the store (no API call), the rest fetched from Alpaca (SIP, 1-min, adjustment raw, market data only, throttled to ~176 req/min) -> `D/pm_bars.db` table `pm`, plus `D/pm_summary.md` and `D/pm_verify.py`. Nothing else was written; `bars_sip.db` was opened read-only.
- SAYS: **100% key coverage, 0 fetch failures** — 21,179 keys (34.2%) from the store, 32,252 (52.1%) freshly fetched, 8,471 (13.7%) genuine no-premarket-trades days (`src='none'`, distinguished from failures, which are logged in `fetch_log` and retried; none remained). `pm_dollar_vol` (sum of close x volume) median $342,686, p90 $14.9M; **17.10% of the 53,431 keys with premarket trades — 14.76% of all keys — are above the ORB cut of $5,816,688**, so the PM$ leg splits the Stage-D population roughly 1:6 and is not degenerate. Provenance is one series, not two: a 20-key spot check re-fetching store-served keys from Alpaca is **20/20 bar-exact** (identical timestamp set, identical h/l/c/v to 1e-6), and an independent recompute (`pm_verify.py`, its own aggregation code) reproduces 12/12 random rows exactly. Caveat on scale: bars are `adjustment=raw`, so pm_high/low/last must not be compared against an adjusted daily file (PLAN §1 price-scale rule).
- NEXT: join `pm_bars.db::pm` on (day, symbol) into the D1 feature table as `pm_dollar_vol` (and the log/percentile forms) and build the two-leg ORB rule `pm_dollar_vol > 5,816,688 AND has_news` from it plus `D/news_presence.csv` — D0 could only test the news leg alone, which was inert. The same backfill must be re-run for `B/candidates4.csv`'s key set once Stage C finishes, since the resting fill adds ~30% more signals (and therefore keys) that are not in `D/table.csv`.
- 2026-09-16 22:35 UTC D0b (`D/d0b_orb_rule.py`, pre-registered in its header): ORB two-leg rule (news AND PM$ > $5.82M) on F6/F8 entries ≥10:00, contract (c). Combo bucket is WORSE than the rest everywhere (F8 N=5 TRAIN −0.21R t −4.2; all families negative on TEST); no ADOPT. Reading: after 10:00 on a ≥5%-range day the newsy high-PM$ names are the crowded/exhausted ones; the rule's home is the 09:35 window on a gap-up universe → Stage E. F6 hold "all" is +0.04/+0.10/+0.11 across splits but TEST SE 0.17 (tail). 42 cells.
- next: Stage C (score5 on candidates4) when B EXIT=0; Stage E causal universe (gap/PM$/PDR at 09:30, parquet store) started in parallel.

## 2026-09-16 — Stage E (H6 causal universe: definition, SIP tape, pass-1 builder — NO scan run)
- RAN: `E/universes.py` (two phases; the 5M-row Databento daily panel -> three universes whose membership uses ONLY data known at 09:30 — **U1 gap>=3% 62,038 symbol-days / U2 prev-day-range>=8% 160,619 / U3 liquid dvol20_med>=$5M 1,512,031**, adv20 and dvol20_med on the `build_daily_panel.py:17` causal convention, 420-day calendar, splits aligned) -> `E/universes.md`, `E/u{1,2,3}_keys.csv`, `E/members.csv`; `E/fetch_causal.py` (detached, ulimit 1.3 GB, nice 10, **DONE + EXIT=0 in 32.7 min**) over (U1uU2) minus `bars_sip.db`; `E/build_candidates_causal.py` (imports `B/build_candidates4.py`'s families + walks through a thin loader, never copies them) + `E/parity_causal.py` -> `E/REPORT.md`.
- SAYS: the fetch served **93,119 of 94,004 keys = 99.06%** (46 `none`, 839 `badsym`, **0 errors**) into parquet+zstd at **3.69 KB/symbol-day — 339 MB, 31.5M bars, 23x smaller than the same keys in `bars_sip.db`'s SQLite**, so `bf_zero2/REPORT.md`'s "~100 GB, beyond this node" was a storage-format artefact (U3 in full would be ~4.6 GB). **Price scale is clean**: Databento daily open vs the 09:30 minute open agrees within 0.5% on 99.0% of 200 random keys (median 0.0000%), and per row `gap_pct` (minute tape) vs `gap_pct_daily` (panel) differ by a median 0.0000 pp. **Parity is exact**: on the 3-day smoke, 1,231 signal rows shared with `B/candidates4.csv` on tape-identical keys match to **max abs diff 0** on sig_m/level/stop/range_so_far_pct/entry_next/rr_2r/entry_rest — 0 rows over 1e-6. The bias H6 names is now measured: **46.5% of causal-universe signals fall in 09:30-10:00 and only 24.8% would have passed the old `range_so_far_pct >= 5` floor**, i.e. the >=5%-range design discarded ~3/4 of the early-window population by construction. Pre-registered for the scan: **60 cells** (5 families x 2 fills x {hold,2r} x {all, news AND PM$>$5.82M, PM$ only}), PLAN §1 gates, TEST read once.
- TRUNCATION + NEXT: (U1uU2) minus `bars_sip.db` was 124,586 keys against the 120,000 cap, so the fetched set is **2025-07-01 onward (93,715 keys)** and **30,871 TRAIN keys of 2025-01..06 are in `E/fetch_keys_overflow.csv`, NOT fetched** — any TRAIN number quoted before `fetch_causal.py --overflow` (~11 min, ~111 MB) is a half-TRAIN number. Next: overflow fetch, then `build_candidates_causal.py` (ONLY after `B/build4.log` ends — one heavy process at a time), then re-run `parity_causal.py` on the full file, then re-point `D/d0_news.py` at the new key set (`D/news_presence.csv` covers only 19.2% of these rows), then score the 60 cells.

## 2026-09-16 — Stage C (score5 on candidates4: 14 families x 2 fills x 5 stop/exit outcomes, contract (c), PLAN §1 gate)
- RAN: verification FIRST and all four passed — row count 3,099,500 lines = 1 header + the builder log's exact final total, 420 days in `build4_state.json`, `head -1` == `COLS` (77/77 identical), `B/parity_smoke.py` on the FINISHED file over ALL 420 days (**913,985 shared `candidates3` rows, 0 missing, max |diff| 0.000e+00 on entry and rr_2r, 0 mismatches on sig_m/entry_m/exit_m/why**, plus 290,719 rows candidates3 could never hold), `B/verify_rows.py 5` ALL MATCH, `C/verify30.py` 30/30 EXACT on 3 random days. Then `C/PREREG.md` (written before any scoring run; ≥10:00 declared PRIMARY, all-day the companion, 115 + 115 gate cells, sensitivities Q/T0/L and paired P1/P2/P3 declared), `C/c0_extract.py` -> `C/pop_c.csv` (447,306 of 3,099,499 rows, identity with the full file re-run and **max abs diff 0.0** over all 107x25 numeric cells), `C/score5c.py` (= `B/score5.py` + reporting only: --queue-ok, gross, SE/MDE, stopP, per-month, --perm-always) x 7 runs, `C/c1_paired.py`, `C/c3_summary.py` -> `C/REPORT.md`.
- SAYS: **0 of 230 gate cells clear G1, therefore 0 clear G2, therefore TEST was NOT read.** Best TRAIN t over the grid **1.93** vs a 500-draw day-label sign-flip null whose 95th pct max-t is **3.88 (p 0.998)** — the grid contains no signal of the size it can detect. Closest miss: `F6 {}` next-open, `2R stop-1%`, ≥10:00 — TRAIN +0.031 R (t 1.43, 19.2 tr/wk, WR 46.9%), VAL +0.063 (t 1.93, 55% green), gross +0.049/+0.082, survives the +3R cap, dies with the top 5% removed; MDE 0.062 R (~1.2 R/wk). **H2 REJECTED**: paired on the 89,826 signals both models fill the resting fill is **−0.058 R net (t −61) and −0.026 R GROSS (t −27)**, VAL the same; the resting-ONLY bursts are genuinely positive (+0.049/+0.053 net, +0.12/+0.13 gross; queue-OK +0.10/+0.11) but the book is worse (−0.083 vs −0.042), and the queue check passes for only 1 of 9 families (0.72–0.96 vs the pre-registered 95%; obtainability 1.000 everywhere) → next-open stays the reference fill. **H1 NOT ADOPTED**: stop−1% +0.0299 R (t 22.9) TRAIN / +0.0277 (t 17.0) VAL with stop rate 28.9→21.3%, close-stop +0.0050/+0.0037 — right sign both splits, stop rate falls, both under the +0.05 bar; the lever lives only where the stop is tight (F13 +0.166, F1 +0.138, F5 +0.077, F10 +0.051 vs F6/F8 +0.004..+0.02). **H8**: hold-to-close is the best exit (−0.019 net / +0.027 gross) and the +2R close-target the worst (−0.100 / −0.051); the ORB lock is **−0.011 R (t −7.8/−6.2) worse than hold** and negative for every config → rejected. **H9 first honest numbers**: F14 second break is the only new family positive on both splits (+0.056 t 1.32 / +0.050 t 0.91), F11 close-confirm ~0 TRAIN / +0.065 VAL (t 2.00) and it does cut the stop rate 14.2→10.7%, while **F12 retest (−0.209/−0.233 R, t −7.7/−6.5) and F13 sweep-and-reclaim (−0.19..−0.35 R, t −6.4..−10.7) are the worst families ever scored here, gross-negative too** — the stop-sweep is a real signature and a destructive ENTRY. Stage A's ≥10:00 window does NOT generalise (the same 107 cells: −0.064→−0.073 net, −0.002→−0.016 gross, 27.9→24.6 tr/wk, only 30/107 improved). Legacy spread reproduces score4 (2 positive cells vs 19). Cells looked at: 230 gate + 575 sensitivity re-scores + 500 paired = **1,305**.
- NEXT: **Stage D1** on `B/candidates4.csv` per the D0 hand-off (the selection question Stage C explicitly cannot answer — PLAN §4 row C: a raw-negative family is what the BF/ORB selection stacks always started from). Carry into it: the next-open fill as the reference, the touch stop, `hold-to-close` as the default exit (not 2R, not the lock), F6 / F8 N=30 / F14 / F11(F6) as the base shapes, F12 and F13 DROPPED, and the PM$ + news two-leg ORB rule from `D/pm_bars.db`. TEST stays unread.

## 2026-09-16 — Stage E side job: causal news presence for the U1uU2 key set
- RAN: `E/e_news.py` (a thin re-point of `D/d0_news.py` at `(E/u1_keys.csv u E/u2_keys.csv)` minus the keys already in `D/news_presence.csv` — same prev-day-15:00-ET -> 14:01-ET window, same seven columns, same 50-symbol chunking and 0.35 s throttle, own `E/news_state.json`), detached, `ulimit -v 1000000`, `nice -n 10`: **162,653 keys / 410 days / 3,686 requests / 43.9 min, log ends EXIT=0** -> `E/news_presence_e.csv` (4.6 MB). Verified by `E/e_news_verify.py` -> `E/news_summary.md`.
- SAYS: **0 fetch failures**, 162,653 unique rows (no duplicates), header byte-identical to D's so the two files concatenate, and unioned with D **coverage of the 198,318 E keys is 100.000%, 0 missing** (D alone covered 35,665 = 18.0%). **0 page-walk truncations in 3,686 requests**, which also clears the same silent-drop risk in D's file (same chunk size and window). Independent re-call of 20 keys one symbol at a time (a different request shape): 10/10 exact on a uniform draw and **10/10 exact on a news-biased draw**, including the `mins_0930_to_1401` minute lists; the 5 rows the first checker flagged were its own artefact (empty `mins` reads as NaN under `na_values=['']`, and all five have `n_0930_to_1401 == 0` — globally 144,056 empty-mins rows == 144,056 zero-count rows). Causal by construction: the window ends at 14:01 and per-row the usable count is `n_prev15_to_0930` plus `#{mins < sig_m}`. Descriptive: pre-09:30 news on **16.8% TRAIN / 14.7% VAL / 12.8% TEST** (15.4% overall), intraday 12.8 / 11.9 / 10.2% — a declining base rate across splits (thinner 2026 tape or vendor coverage, not settled), so any model using it must be walk-forward and the level shift must not be read as a regime signal.
- NEXT: join on `(day, symbol)` into the Stage-E candidate table and score the 60 pre-registered cells (5 families x 2 fills x {hold,2r} x {all, news AND PM$>$5.82M, PM$ only}) under PLAN §1's gates; TEST stays unread. Nothing here is a result — this is a feature column, and the news share is NOT evidence of an edge.

## 2026-09-16 — Stage D1 (H4 feature/selection program on `candidates4.csv`)
- RAN: `D1/PREREG.md` (written before any run: 48 model cells = 4 families F6 {} / F8 N=30 / F14 / F11(F6) x 3 models x 2 selections x 2 stop/exit targets, + 5 univariate two-leg cells, ALL-DAY population, next-open fill, contract (c)), then D0's pipeline re-pointed: `d1_table.py` (110,030 rows from `C/pop_c.csv`), `d1_features.py` (**53 causal features**, incl. the premarket dollars and news D0 lacked), `d1_parity.py` (**20/20 cells vs `C/score5_results.csv`, max |dn| 0, max |d meanR| 0.0**), `d1_model.py` (walk-forward monthly refits 2025-10→2026-09, real / Nagel-reversed / shuffled tapes, 11.5 min), `d1_eval.py`, `d1_perm.py` (500 draws), `d1_orb.py`, `d1_tables.py`, plus an unplanned ablation `D1_TAG=_clean D1_DROP=pmnews`.
- SAYS: **the stage's headline number is a look-ahead, not a book, and the mandatory checks caught it before TEST.** The declared run's best cell (`F6 {}` reg S2, VAL **+0.415 R, t 5.10, 86% weeks green, 8.4 R/wk, every predicted month positive, decile rho +0.90**) is driven by `f_log_pm_dollar`, whose **availability** is keyed to `D/pm_bars.db` = the (day,symbol) set that produced a **≥10:00** candidates3 signal: for a 09:35 signal "premarket dollars known" means "this symbol-day also signalled later". Early rows on symbol-days with a later signal **+0.387 R** vs **−0.336/−0.456** without; missing rate 33.1% before 10:00 vs 1.5% after; the `missing` bucket is −0.28..−0.46 R at t −6..−16 in **every** family and split, while the genuinely causal "no premarket trades" category (`src='none'`) is **+0.024**. Cost decomposition cleared the cost term first (flat 0.017-0.033 R everywhere; the lift was gross). **Gates: G1 10 of 48 contaminated — all 10 also profitable on the reversed tape (up to +0.177, t 4.60) — and the transparent baseline clears G1 in 0 of 16, so no ship candidate either way; with pm/news dropped G1 = 0 of 48, the best VAL cell is +0.211 vs FCFS +0.163 (selection worth +0.05 R), all VAL cells die with the top 5% removed, and TEST (read once, both pipelines, after the rule was frozen in REPORT §F) returns +0.006 / −0.057 / +0.074 / −0.047 on the four frozen clean cells, 13 of 48 positive.** The two-leg ORB rule (news AND PM$ > $5.82M) is negative or flat in **0 of 20** family x band decisions all day — D0b's ≥10:00 result generalises; the only consistently positive bucket is `news_only` (TRAIN +0.078..+0.093 t 2.0-2.8, TEST +0.091/+0.136/+0.194), not pre-registered and NOT adopted. VAL MDEs 0.12-0.20 R (~2-4 R/wk at 4 slots). 1,353 cell-instances.
- NEXT: two rules for the rest of the program, both earned here: (1) **audit the availability of every feature whose coverage is < 100%, per split AND per time-of-day band — coverage built from another stage's key set is a look-ahead even when the feature's VALUE is causal** (`D/pm_bars.db` is safe only for D0's own population; candidates4's 52.9%-empty `pm_dollar_vol` must never be a feature on a mixed-time population); (2) **the tail test cannot catch a cohort leak** — every contaminated survivor passed top-5%-removal and the +3R cap; only the missingness audit found it. Stage E's scan should use `E/news_presence_e.csv` (causal key set) and, if premarket dollars are wanted, backfill them over E's FULL key set with `src='none'` recorded as a real zero; the `news_only` direction deserves its own pre-registration there.

## 2026-09-17 — Stage E SCORING (the 60 pre-registered cells on the causal universe, + the pre-registered `news_only` bucket)
- RAN: verification FIRST and all five passed — `build_causal2.log` `EXIT=0`, 410 days in `build_causal_state.json`, **77,574 (run-1) + 506,195 (run-2) = 583,769 = the file's data rows exactly**, header byte-identical to `B4.COLS`+11 extras (88/88), the **overflow fetch had completed** so TRAIN is NOT truncated, and `parity_causal.py` over the FULL 410 days (217,576 signal rows shared with `B/candidates4.csv` on tape-identical keys: `sig_m`/`stop`/`range_so_far_pct`/`next_entry`/`next_entry_m`/`next_rr_2r` **max abs diff 0.0, 0 rows over 1e-6**; residual float32 noise on 13 `level` values, 0.006%); survivorship residual 1,510 of 198,318 keys = **0.76%**. Then the PLAN §1 AVAILABILITY AUDIT (`E/e_avail.py`) before any number, `E/e_score.py` (60 cells + U1/U2/queue-OK sensitivities + a 500-draw permutation), `E/e_follow.py` (the H6 floor split, time bands, `news_only` booked, PM$-decile monotonicity, Simpson's, the Stage-C diff), `E/e_f6floor.py` (the one post-hoc gate pass; TEST read once after the rule was frozen in `E/REPORT.md` §7), `E/e_headlines.py` -> `E/REPORT.md` §8.
- SAYS: **0 of 54 scoreable cells (60 declared) clear G1, therefore 0 clear G2, therefore the grid's TEST was not read.** Best TRAIN cell +0.018 R at t 0.15; the 500-draw day-label sign-flip null gives observed max t **0.15 vs a null 95th pct of 3.81, p = 1.000**. **H6 is refuted in the direction opposite to the hypothesis**: the causal universe is −0.066 R/trade WORSE than the ≥5%-range universe on the 18 matched cells, and the rows the old `range_so_far_pct >= 5` floor discarded (60–80% of the causal population) are the LOSING rows in 8 of 10 family×split comparisons (7 clearly, F13 TRAIN by a hair) (F6 {} +0.090/−0.114 TRAIN, +0.209/−0.046 VAL; F8 N=5 −0.005/−0.144 TRAIN), with stop rates 28% vs 59%. The 09:30–10:00 window is the WORST band for every family on the universe built to give it a fair test. Next-open beats resting 5/5 and hold beats 2R 5/5 (Stage C reproduces). **The ORB two-leg rule (news AND PM$ > $5.82M) is negative in its own 09:35 window on a gap-up universe — TRAIN 5/5, TEST 5/5, and 09:35–10:00 TRAIN 5/5 — so D0b/D1's finding is now closed, not merely untested**; `pm_only` alone is negative 5/5 too. The pre-registered **`news_only`** bucket passes the sign-agreement rule in **1 of 5 families** (F8 N=5: TRAIN +0.061 t 2.54, VAL +0.026, TEST +0.073 t 2.15; survives the pm-unknown ablation and is not a Simpson's artefact) but is **anti-monotone in PM$ inside the news bucket (TRAIN decile rho −0.78)**, is negative in absolute R in every split, and clears G1 in **0 of 9 booked cells** — a crowding penalty on high-PM$ newsy names, not an edge. `has_news` coverage is **100.00%** (no D1 leak possible); `pm_dollar_vol` is 89.0% and its missingness flips sign across splits at 0.03–0.05 R (not D1's −0.28..−0.46 R signature) and is NOT a proxy for the old fetch's key set. One post-hoc cell cleared the arithmetic (`F6 {}` next/hold restricted to `range_so_far_pct >= 5`: TRAIN +0.090 t 2.03, VAL +0.209 t 2.80 77% green); frozen in writing, **TEST read once: +0.096 R, t 0.67, 43% weeks green, 2 of 4 months red, August alone is the whole book**; it dies with the top 5% removed on all three splits (−0.139/−0.062/−0.227) and under a +3R cap on TEST, perm p 0.936 — reported as a failure exactly as pre-committed. **No ship candidate.** Smallest per-trade effect the headline tests could see: **0.083–0.154 R (2.0–6.8 R/week at 4 slots)**. ~748 cell-instances declared, ~637 reported.
- NEXT: H6 is closed — keep `range_so_far_pct >= 5` as a CAUSAL FILTER (it is live-computable in `hod_break_engine.py` from the streamed bars) and stop treating it as a universe bias; drop the ORB two-leg rule from the candidate-feature list for this program (5/5 negative in its designated window); record the one usable sentence for `research/orb_machine_rules.md` — *within newsy names, more premarket dollars is worse, TRAIN decile rho −0.78* — as a rulebook note, NOT a book. U3 (the unbiased liquid slice, 1.51M symbol-days, ~4.6 GB, ~7.5 h) remains the only untried universe and is now the cheapest remaining H6 question; Stage F's owner memo should carry the closest miss per hypothesis and these MDEs rather than a candidate.

## 2026-09-17 — Stage Q (obtainability + fill-realism audit of the QQQ noise-area sleeve, RESULTS rows 1/1b)
- RAN: `Q/zsim.py` (re-implementation of the Zarattini noise-area rule, diffed TRADE BY TRADE against `research/lit_review_2026/test_zarattini_spy.py`: SPY 2,443 / QQQ 2,362 trades, max|diff| 0; loaders checksum-identical via `Q/loader_check.py`), then `Q/step1_rederive.py` (row 1b reproduced: QQQ OOS 10.14 bps/day dyn, t 1.59, SR 0.987 1x, 8.35%/yr), `Q/step2_live.py` (6 execution scenarios x SPY/QQQ/TQQQ + fill-gap and clock-window attribution), `Q/step5_extras.py` (cost curve, tails, power, split check). Outputs `Q/REPORT.md`, `Q/DRYRUN_SPEC.md`, `Q/step*.csv`. No config/service/order touched.
- SAYS: **the live-convention fill is NOT what threatens this sleeve.** Paper fills at the CLOSE of the check bar (not at the band level; the close overshoots the band by a median 12.9 bp, so it is pessimistic on price and optimistic on time by <=60 s). Replacing it with decide-on-closed-bar + market order at the NEXT bar's open moves QQQ OOS 5.84 -> 5.83 bps/traded day; the two prices differ on 82% of legs but by a median of **0.0 bp** (mean +0.003, p90 0.53), and every fill is inside its bar. The resting-limit-at-the-band idea is unharvestable here: it fills at the minutes the semi-hourly rule deliberately does not trade = the every-minute variant already known dead (SR 0.10 IS). What DOES threaten it: cost (0.5 bp/leg -> OOS **4.57 bps/traded day, t 1.27**; 1.0 bp -> 3.06, t 0.85; **breakeven 2.01 bp/leg**), the tail (**top 5 OOS days = 102% of the whole OOS return**; top 1% of days removed -> −0.53 bps/day; a +1%/day cap -> +0.03; **2025-04 alone = 51% of the 33-month total, 2025-04-09 alone = 45%**), and power (**OOS MDE 5.75 bps/day vs an observed 2.60**). $ at $60K OOS: QQQ 1x **+$321/month mean, −$2,243 worst month, MDD $6,369, 22/33 green**; QQQ 3x notional +$962/−$6,729/MDD $17,573 (intraday only, so day-trading buying power, **zero margin interest**); **TQQQ 1x beats QQQ 3x OOS on both axes** (+$1,092/month, MDD $15,845 — the daily-reset drag does not bite a book flat at every close), though TQQQ's IS MDD is 42.9%. Also found and fixed: `etf_1min.db` holds RAW bars and **TQQQ split 5x in the sample incl. 2025-11-20 inside the OOS window** — factors derived (not assumed) as `(TQQQ open/prev_close)/(1+3*QQQ gap)` = 0.4996-0.5000 / 0.3333, with 2020-03-16 (1.0225) correctly identified as the COVID gap, not a split; un-adjusted the TQQQ OOS row read 16.86 instead of 15.56 bps. First-30-minutes share of P&L is **0.0% by construction** (first decision 10:00); the last 30 minutes are +9.8% of IS P&L and **−20.5% of OOS**. 36 distinct cells for this sleeve (9 prior + 27 here); best OOS t across all of them is 1.67 (VM 1.2).
- NEXT: nothing is enabled and nothing should be. `Q/DRYRUN_SPEC.md` is written as a spec only (minute loop, StopMonitor light bar handler, 09:30 session state with its construction times, kill rails sized off the OOS distribution — daily −$750 / 5-day −$1,800 / month −$2,300 / DD −$6,400 at 1x on $60K — the `[QQQN]` log-line contract that lets an EOD check re-simulate every one of the 12 decision minutes, and the measurables). Its stated purpose is to measure EXECUTION (median fill slip vs the next-bar open must stay under 1.0 bp/leg or the arithmetic dies at the 2.01 bp breakeven), never to confirm the edge: 20 sessions carry a standard error of 12 bps/day against an effect of 2.6. Still owed before any go-live number: a blind second-agent rebuild from `Q/REPORT.md` §2's prose (this stage's diff was a re-implementation by a model that had read the original) and real NBBO rather than a bp assumption.

## 2026-09-17 — Stage G (the SHORT side, H12): 6 mirrored short families on the borrowable causal universe
- RAN: `G/PREREG.md` FIRST (universe = U1uU2 restricted to price >= $10 / 20-day median dollar volume >= $10M / `asset_class == stock`, plus the M18 attention list on the same rule; 6 family-configs S1 gap-fade / S2 ORB-breakdown N=15,30 / S3 green-to-red / S4 HOD-rejection / S5 attention fade, x 2 exits x 2 universes = **24 gate cells** + 132 declared sensitivities; contract (c) mirrored + a borrow row; SSR days excluded; TEST for G2 survivors only) — the two amendments it carries (S5's population and its declared stop) were written BEFORE the first run. Then `G/g0_universe.py` (72,793 symbol-days / 410 days / 2,690 symbols; SSR = `low_{t-1} <= 0.90 x close_{t-2}` from the same Databento panel; the M18 selection reproduced field-for-field from `test_open_fade.py`), `G/build_candidates_short.py` (the long-side machinery IMPORTED — `build_candidates.py` + `B/build_candidates4.py` — with the detectors and the walk written as mirrors: stop fires on `high >= stop` and fills at `max(stop, open)*1.001`, the -2R target on a bar CLOSE, no chase BELOW `level*(1-0.6%)`), `G/verify_rows.py`, `G/score_short.py` (= `B/score5.py`'s cost/book/gate code + a `cover` exit ratio and a borrow term), `G/g_extras.py`, `G/g_m18.py` -> `G/REPORT.md`.
- SAYS: **0 of 24 cells clear G1, therefore 0 clear G2, therefore TEST was NOT read.** Best TRAIN cell `S5 UB 1030` +0.0444 R (t 0.22) at 2.6 tr/wk (fails the >= 5 tr/wk bar by itself); best cell meeting the frequency bar `S1 UB hold` +0.0261 R (t 0.65, 9.4 tr/wk, VAL +0.0235 t 0.37, 64% weeks green, survives the +3R cap, **dies ex-top-5% at -0.083**); a 500-draw day-label sign-flip null gives observed max TRAIN t **0.65 vs a null 95th pct of 3.53, p = 1.000**. Gross the short side is ~flat (+0.108 / +0.057 / +0.032 / +0.019 / +0.005 / -0.007 R by family on UB) and the cost contract charges 0.052-0.120 R — the same near-zero-gross / negative-net shape Stages C and E found long, sign flipped. **The `range_so_far_pct >= 5` floor beats no-floor in 12 of 12 short pairs** (Stage E found it 8 of 10 long) — it is direction-agnostic and is this program's most reliable single filter. Hold-to-close beats the -2R target in 8 of the 10 S1-S4 pairs. S4 (HOD-rejection) and S3 (green-to-red) are the destructive entries (-0.10..-0.16 R at 39-53% stop rates) — the mirror of Stage C's F12/F13 finding: a consolidation break taken in EITHER direction is where the stops are. **SSR blocks 6.7-14.8% of the pattern families' signals and 41.0% of S5's**; including SSR days changes no verdict. The borrow charge (5 bps locate + 1%/365) costs ~0.018 R and moves no cell. **The M18 open fade does NOT survive into the borrowable band at a 09:35 entry**: on M18's own spec (no stop, no R filter) the attention names' 09:35->10:30 short return is -4.6 bps TRAIN / +32.9 VAL vs a control at -5.8 / -3.7 and an MDE of 38 / 53 bps — M18's TRAIN fade is a first-five-minutes ($-37 of its $-69 at $10-50M is printed before 09:35) and small-dollar-volume effect, and the borrow rule deletes both; attention-minus-control in R is nonetheless real and positive (+0.173 TRAIN / +0.204 VAL). Verification passed in full: 187,774 lines = 1 header + the builder's exact total, 410 days, header 60/60 identical to `COLS`, **1 symbol-day of 72,793 with no tape (0.0014%)**, price-scale check vs the panel median 0.000002%, and **42/42 rows rebuilt row-by-row by an independent implementation** with its own loader and plain-Python loops. Obtainability: **100% of entry fills inside their bar and 0.00% of exit fills on the favourable-impossible side in every family** (10-26% fall outside on the side that COSTS the short). Two defects found and fixed mid-stage and reported as such: an empty `attn_grp` read as NaN made `not NaN` False so S5 was emitted for every member (the scorer filtered correctly — the 24-cell table is byte-identical before and after), and the first M18 bps block ran on the `r_pct >= 1.0` population, a stop-distance filter that for S5 selects names that had already dropped. Smallest per-trade effect visible at 80% power: **0.056-0.616 R per trade (0.9-3.3 R/week at 4 slots)**. **200 cell-instances looked at, 156 declared in advance.** Borrow AVAILABILITY is NOT modelled anywhere — stated loudly in `G/REPORT.md` §8.
- NEXT: nothing live, and no short candidate. Carry forward two things only: (1) `range_so_far_pct >= 5` is a direction-agnostic, live-computable filter — treat it as such in any future stage rather than as a universe artefact; (2) the M18 veto stands but is now BOUNDED — the tradable part of the fade is pre-09:35 and in the $2-10M band, i.e. outside any borrowable short book; record that as a rulebook note, not a book. The cheapest un-run question on either side remains Stage E's **U3 unbiased liquid slice** (1.51M symbol-days, ~4.6 GB, ~7.5 h): every family in Stages C/E/G was measured on a population pre-selected by a gap, a prior-day range or an end-of-day range, and that is a conditioning choice, not a law.

## 2026-09-17 — Stage H / QQQ noise-band sleeve (bottom-up loser analysis on the DAY unit)
- RAN: `H/QQQ/base.py` (Q anchor reproduced EXACTLY — IS 6.48 bps/traded day t 3.32, OOS **4.57 t 1.27** — then the day/trade tables on the H splits TRAIN 2016-01..2022-12 / VAL 2023-01..2024-06 / TEST 2024-07..2026-09), `anatomy.py` (concentration, worst/best 30 days annotated, 15 causal-feature quartile tables with both TRAIN halves, weekly terciles, path shape, autocorrelation, entry-minute and exit-mix), `PREREG.md` written BEFORE the grid (7 filter families with mechanisms, coarse cuts, the era decision rule), `step2_train.py` + `step2b_train.py` (28 grid cells + the vetoed-bucket era test), `step3_robust.py`, the stack FROZEN in writing in `PREREG.md`, then `step4_val.py` (VAL read once) and `step5_diag.py` (power, decomposition, permutation, one labelled post-hoc cell) -> `H/QQQ/REPORT.md` + `final_book_days.csv`. No config/service/order touched.
- SAYS: **two causal 09:31-ET filters were found, frozen, and FAILED VAL; TEST was NOT read.** The TRAIN mechanism is real and era-consistent — the sleeve loses on days with a NARROW band (`band_w = (UB-LB)/open` at 10:00 < 0.8 %, vetoed bucket -1.34 H1 / -1.85 H2) and a small overnight gap relative to it (`|gap|/band < 0.20`, -0.20 / -1.86); stacked they take TRAIN from **6.24 -> 11.04 bps/traded day** (t 2.91 -> 3.55), MDD 9.9 -> 4.7 %, worst month -405 -> -371, both halves up, and — uniquely in this program — the **tails turn positive** (ex-top-5 % days +0.32 vs -3.79; capped at +1 %/day +4.35 vs +1.01). On VAL the mean improves the same way (7.82 -> 12.54) but **the vetoed days are PROFITABLE (+1.39 bps/day, n=94)**, so the pre-committed conjunction fails. The decomposition is the finding: the vetoed set sums **+59 bps on TRAIN (+0.13/day)** and **+131 on VAL**, i.e. the whole "improvement" is a **DENOMINATOR effect** — the stack's total falls (TRAIN 6,515 -> 6,456; VAL 1,736 -> 1,605) and at $60K it COSTS money in both splits ($469 -> $465/month TRAIN, $584 -> $541 VAL) while improving every ratio. My pre-registration required the mean to rise and not the sum — that defect is recorded, not repaired. The 2x2 shows only the "neither condition" cell is genuinely bad (TRAIN -5.18, n=171) and it **flips to +3.93 on VAL** (n=47); the post-hoc AND-veto that repairs the arithmetic on TRAIN (sum +7,401) fails the identical VAL test (+3.93) and is reported, not adopted. Permutation p 0.0118 vs a Sidak alpha of 0.0017 at 30 rule variants — not significant after multiplicity even before VAL. Five families died on the era rule or on TRAIN: **F3 entry-cap REJECTED** (max 1 entry/day 6.24 -> 5.15 — the step-1 "losing days have more entries" association is reverse causality), F4 MA20/MA5 side gate, **F5 "only crossings before 12:00" REJECTED** (4.72, worse in both halves), F6 vol floor and F7 prior-5-day movement floor (their vetoed buckets are POSITIVE in both halves). Refuted before the grid: "skip after a gap larger than the band" (the largest gap/band quartile is the BEST, +10.77) and "stand down after N losing days" (after a loser +11.00 vs -0.04 after a winner, lag-1 corr -0.13). No FOMC/CPI calendar and no VIX exist on this node for 2016-2022 — stated as skipped, rv20/sig14 used instead. Smallest effect visible: TRAIN book MDE **6.01 bps/traded day** against an observed 6.24 (the in-sample edge sits ON its detection floor); VAL veto-bucket MDE **14.18 bps/day** against an observed +1.39. 30 rule variants / 53 cell-instances here; 66 / 89 for the sleeve including Q.
- NEXT: nothing is enabled and nothing should be; the sleeve stays where `Q/REPORT.md` left it (dry-run spec only, unfiltered). TEST remains unread for this sleeve apart from one disclosed unfiltered aggregate (554 days, 313 traded, 3.97 bps/traded day) printed by the bookkeeping script. The day-level question is now power-bound, not idea-bound: at 1,044 TRAIN / 222 VAL traded days no day filter worth a few bps is resolvable, so the only honest next move is more N (the same rule on DIA/IWM/SPY/SOXL as a pooled panel, or history before 2016) rather than another cut of these 2,124 days — and the two claims worth carrying to any such run are that the losing day is the day that crosses the band and does NOT trend (corr(day bps, trend-after-first-cross) +0.71, efficiency ratio 0.05 on losers vs 0.13 on winners) and that re-entries are a symptom, not a cause.

## 2026-09-17 — Stage H / F14 second break + F8 N=30 opening-range break + F11 close-confirmed F6
- RAN: `H/F14_F8_F11/h_extract.py` (the three books out of `C/pop_c.csv`), `hcore.py` (Stage-C contract (c) + `run_book(12,4)` re-implemented), `h0_parity.py` (**18 of 18 Stage-C cells reproduced EXACTLY, max|dn| 0, max|d meanR| 0.0**, plus F11's `>=10:00` twin 6/6 exact), `h1_anatomy.py` per book (availability audit, concentration, worst-20 days with market context, winners-vs-losers on 30 causal columns, path anatomy, 138/137/135 era cells), `h_eval.py` (the coarse filter grids, both booking conventions, the stacks), three `FREEZE_*.md` written BEFORE any `--val` run, `h_valdiag.py` (per-filter VAL contribution), `h_perm.py` (500-draw day-level sign-flip null over all 83 TRAIN cells), `h_nofloor_extract.py`+`h_nofloor_score.py` (the declared no-floor twin, re-extracted from `B/candidates4.csv`). No config, service, cache or order touched.
- SAYS: **all three books reach profit on TRAIN and all three FAIL the pre-registered VAL rule; TEST was NOT read for any of them.** F14: +0.0564 -> **+0.1398 R/trade, t 2.41**, both halves +0.12/+0.16, MDD −31.3 -> −19.8, on three vetoes with shipped-rule mechanisms (level >0.8% above the day open; level at/above session VWAP = BF's `vwap_gate`; `adv20` exists = ORB's G1 short-history veto) — and on VAL the book falls to **+0.0096 from +0.0503** while **6 of its 7 era-passing vetoed buckets flip from −0.04..−0.34 on BOTH TRAIN halves to +0.11..+0.34** (the 7th has no VAL instance) — F14 is the only book with ZERO surviving buckets. F8 N=30: +0.0079 -> **+0.0581, t 1.62**, MDD −40.5 -> **−9.0**, on no-wrapper + ORB touchgo Rule M (`sig_close_pos>=0.5`) + `gap_pct<3`; VAL **−0.0340** (from −0.0107); **7 of its 10 era-passing buckets DO keep their sign on VAL** — but the three that flip are exactly the two mechanism filters in the stack (M1 +0.033, G1 +0.043) while the seven that persist are the calendar/day-context cells declared non-primary before the VAL read. F11(F6): −0.0115 -> **+0.0804, t 1.62**, 57% weeks green, MDD −50.4 -> −19.4, on ORB's PDR veto (`prev_day_range_pct>=5`) + a no-chase rule (`sig_body_pct<1.0`); VAL **+0.1517 vs +0.0775** — it improves, 55% weeks green — but `B1`'s vetoed bucket is **+0.0345 on VAL**, so the frozen "each vetoed bucket negative on VAL" leg fails and TEST stays unread. The search-adjusted permutation settles it: over 83 TRAIN cells the observed max |t| is **2.44** against a null 95th pct of **3.85** and a null MEAN of **3.04**, **p = 0.932**. Two methodological findings worth more than the null: (1) **the booking convention is a lever, not a detail** — `run_book` refilling a vetoed pick's slot flips M1 on F8 N=30 from +0.026 to **−0.031**, so every veto here is scored ORB-style (post-ranking, NO refill) and both columns are printed; (2) **the no-floor twin is a look-ahead and measures as one** — dropping `range_so_far_pct>=5` on this end-of-day-range universe gives F14 **+0.2996 R at t 6.75**, F8 N=30 +0.2097 at t 6.70, F11 +0.2620 at t 3.86, with the below-floor rows ALONE at +0.28..+0.39 — the exact opposite sign to E/REPORT §8.4's causal-universe measurement (below-floor rows are the LOSERS there), i.e. the floor is the causal membership guarantee and removing it buys the day's outcome. Availability audit rejected two features before use: `pm_dollar_vol` (coverage 35-47%, missing bucket +0.38/+0.14/+0.09 R vs present −0.10/−0.18/−0.19 — D1's leak signature on a key set that is not these books') and, for F14/F11, `news_pre` (78%/92% coverage, present-minus-missing +0.22/+0.25 R). Smallest per-trade effect the VAL tests could see: **0.156 R (F14) · 0.158 (F8 N=30) · 0.252 (F11)** = 1.9 / 1.0 / 3.3 R per week at 4 slots. Cells looked at in this sub-stage: **9 + 11 + 9 single-filter cells, 8 stack prefixes, 2 booking conventions, 4 declared sensitivities = 83 TRAIN book cells + 410 era-consistency cells**.
- NEXT: nothing is enabled and nothing should be. Three things are worth carrying, all pre-registrable with TEST still virgin for them: (1) **`prev_day_range_pct >= 5` on F11(F6)** is the only veto in the sub-stage whose bucket is negative on TRAIN-H1, TRAIN-H2 AND VAL (−0.184 / −0.063 / −0.086) and whose book improves on both splits (+0.041 TRAIN, +0.112 VAL vs +0.078) — it is ORB's shipped PDR veto transferring to a second family, and it is the one hypothesis here that deserves its own pre-registration rather than a re-read of this one; (2) the **F11 `2R stop-1%` secondary exit** is the least tail-dependent cell produced anywhere in Stage H (TRAIN +0.0623 t 1.82 with H1 +0.075 / H2 +0.050 and ex-top-5% **−0.037**; VAL +0.0632, ex5 −0.031) and should be the exit any future F11 work starts from, not `hold`; (3) **a breakeven / partial rule on F11 and F14** — 30.8% and 25.2% of stopped trades had +0.5R on the table first and 54.8% / 100% of their stops are wicks (the bar trades through the stop and closes back above it); that is the one SHAPE the anatomy points at and the 3-veto budget never tested. Counted exactly (`vetoed_persistence.csv`): of 29 filter cells, 23 have a vetoed bucket negative in BOTH TRAIN halves, 22 have a VAL instance, **9 hold their sign and 13 flip** (F14 0/7, F8 N=30 7/10, F11 2/6) — and the F8 N=30 companion set (`spy_gap not in [0,0.3)`, `spy_vs_sma20 not in [0,2)`, not-Friday) must stay non-primary until a mechanism sentence exists for it, which is why its persistence is reported and not acted on.

## 2026-09-17 — Stage H / F6 red-to-green (+ its stop−1% twin): bottom-up loser analysis on the two universes
- RAN: `H/F6/h_extract.py` (the F6 rows and the week denominators out of BOTH population files — **Q = `E/candidates_causal.csv`**, the causal U1uU2 universe the METHOD's reference numbers come from, 32,922 rows, and **P = `C/pop_c.csv`**, the >=5%-range-day universe, 12,018 rows), `h_core.py` (Stage-C contract (c) + `run_book(12,4)` restated in code), `h0_parity.py` (**every published anchor reproduced exactly**: E §7/§8.8 TRAIN +0.0896 n 1034 t 2.03 and VAL +0.2088 n 512 t 2.80; C §4.1 all-day TRAIN +0.0510 t 1.34 / VAL +0.1629 t 2.46; and E §8.4's floor twin +0.0896 vs **−0.1144** TRAIN, +0.2088 vs −0.0457 VAL — the no-floor twin is only runnable on Q because `c0_extract.py` already bakes the floor into P's F6 rows), `h1_anatomy.py` (availability audit, concentration, worst-20 days with SPY/IWM context, losers-vs-winners on 28 causal columns, path anatomy, 561 era-split bucket cells), `h2_filters.py` + `h2b_stacks.py` (26 coarse single cuts x 2 universes, 8 stacks x 2, leave-one-out, the vetoed cohort), the stack **FROZEN in writing in `H/F6/REPORT.md` §4 before VAL**, then `h3_val_test.py` (VAL once + the 500-draw permutation) and `h4_secondary.py` (the 2R/stop−1% twin). `H/F6/REPORT.md`, `h_final_book.csv` (2,784 booked trades, TRAIN+VAL, both universes). No config, service, cache or order touched.
- SAYS: **the book reaches profit on TRAIN and FAILS the pre-committed VAL rule; TEST was NOT read.** Frozen stack = `prev_day_range_pct>=8` (ORB's shipped PDR veto) + `vwap_dist_pct>0` (BF P1's gate) + `close_over_level_pct=(sig_c/level−1)*100 <= 0.25` (a no-chase rule: the fill is the NEXT bar's open, so the further the signal minute closed above the reclaim level the more of the move we have already bought while the stop, the day's low, has not moved). TRAIN Q **+0.0896 -> +0.1420 R/trade, t 2.03 -> 3.12**, 20.3 -> 17.2 tr/wk, MDD **−26.3 -> −15.6R**, +3R cap +0.012 -> +0.072, both halves up (+0.128/+0.154); TRAIN P +0.0510 -> +0.0988 (t 1.34 -> 2.36), MDD −37.5 -> −17.9. On **VAL the stack is a COST**: Q **+0.2088 -> +0.0877** and the vetoed cohort pays **+0.2285** (77% weeks green); P +0.1629 -> +0.1489, vetoed cohort +0.1356. Per filter: **`pdr>=8` is the only one that replicates** (P VAL +0.1629 -> **+0.2464**, t 3.23, 82% weeks green, its vetoed bucket ~0 at +0.0037; a no-op on Q because Q's own 09:30 membership rule already contains it — **that is most of why Q's book is twice P's**), while both rules the loser analysis itself produced INVERT: on P VAL the below-VWAP cohort, which lost 0.08–0.26R across both TRAIN halves at a 53–74% stop rate, paid **+0.727 R/trade** (n 68, t 2.24), and the `col<=0.25` bucket went +0.1715 on Q VAL. The stop−1% twin fails identically (Q TRAIN +0.051 -> +0.077, VAL +0.080 -> **+0.039**). The search-adjusted permutation over the stage's 36 book cells had already flagged it on TRAIN: observed max t **3.12 vs a null 95th pct of 3.24, p = 0.074**. Two findings worth more than the null: (1) **universe P carries D1's availability leak and it is large** — news coverage 80.6% there, "news known" worth **+0.1415 vs −0.3252 R** and "premarket known" +0.1438 vs −0.1347, so every news/premarket column was struck out before the grid (on Q news coverage is 100% by construction of `E/e_news.py` and the premarket gap is 0.06R with no band structure); (2) **the day-level shape of filter is dead before it is tried** — the worst 5% of days carry only **21%** of the loss while the best 5% carry **113%** of the profit, and the 20 worst days have no shared market context (SPY closed up on 6 of 20, regime A on 15 of 20). Path anatomy closes the exit-side door too: only **17% of stopped trades ever saw +1R** (median 78 min to the stop, 37% of stops after 2h), against Stage A's measured best stop/exit variant of +0.023R. Smallest per-trade effect visible at 80% power: **VAL 0.175–0.209R (4.1–6.1 R/week at 4 slots), TRAIN 0.107–0.127R**; the stack needed to show ~+0.05R. **~867 cell-instances in this sub-stage** (561 buckets, 86 filter/stack books, 44 VAL, 36 parity, 12 secondary, ~128 diagnostics); TEST contributes 0.
- NEXT: nothing is enabled and nothing should be; F6 stays exactly as Stage E left it and **Stage H adds no filter to it**. TEST remains virgin for this book — spend it once, on ONE pre-registered hypothesis, and the only one that has earned a pre-registration is **the PDR veto as a cross-family rule**: it is the third independent book in this program (ORB shipped, F11(F6) in `H/F14_F8_F11`'s NEXT, F6/P here) where `prev_day_range_pct` is negative on TRAIN-H1, TRAIN-H2 **and** VAL and lifts the book on both splits — that is a transfer, not a fit, and it should be tested as "the shipped ORB threshold applied unchanged to the long 1-minute families on the causal universe", never re-tuned. Two things to carry into any such run: the causal `range_so_far_pct>=5` floor is worth **+0.20R TRAIN / +0.25R VAL** on F6 and remains this program's most reliable single filter (Stage G measured the same sign on 12 of 12 SHORT pairs), and any filter found by staring at these losers must be assumed to invert until a second, non-overlapping universe says otherwise — here two such filters passed the era rule, the population check, monotonicity AND an independent Stage-A corroboration on a different tape, and still inverted.

## 2026-09-17 — Stage H / F6_sizing: how much money the F6 red-to-green book can carry (capacity, no selection)
- RAN: `H/F6_sizing/f6_load.py` (11,906 F6 rows out of `C/pop_c.csv` under B/score5.py's exact `next`-fill population and contract (c); **the C anchors reproduce to the digit** — hold TRAIN +0.0510 t 1.34 / VAL +0.1629 t 2.46, 2R close-fill +0.0273 t 1.05 / +0.0702 t 1.72), `f6_bars.py` (fill-bar and trailing-5-minute dollar volume for every population row, bar source = `build_candidates.py::load_bars` precedence, i.e. `data/cache.db` FIRST then `bf_zero/bars_sip.db` — **bars_sip.db alone covers only 41% of these symbol-days** and using it alone silently drops 59% of the book; coverage 100.0%, fill inside its own bar 100.00%), `f6_sizing.py` (64 descriptive cells: 36 book re-runs = 2 exits x 6 risk levels x {as-is, 1% cap, causal 1% cap}; 18 cost cells; 10 band cells). `H/F6_sizing/REPORT.md`, `trades_f6_sizing.csv` (2,156 booked hold-exit trades with shares/notional/participation at every risk level), `pop_f6_liq.csv`, `weekly_dollars.csv`. Nothing outside the directory written; no config, service, cache or order touched.
- SAYS: **the honest capacity is ~$250-$400 risk/trade and the throughput SATURATES at ~$1.4K-$2.0K/month (TRAIN+VAL pooled), not the $3.5K-$17K naive "weekly R x risk" gives.** The median booked trade carries only **$226** of risk at 1% of its own 5-minute tape (p25 $45, p75 $1,095); at $400 risk the median participation is **1.77%** and only 40.4% of trades are under 1% (at $100: 0.44% median, 64.1% under 1%). Capping at 1% and re-booking so slots refill: pooled TRAIN+VAL $/month = 474 ($100) / 1,426 ($250) / 1,700 ($400) / 2,013 ($700) / 1,526 ($1,000) / 1,705 ($2,000) — **flat from $250 up**, because raising risk deletes exactly the trades with the edge: the bands that CAN carry $1,000+ are $50-100 price (mean net R **−0.144**) and 10M-50M ADV (**−0.012**), while the edge sits in $5-10 / <500K-ADV names whose median capacity is $73 / $53. The live-computable cap (four PRIOR bars x1.25, since the fill bar's own volume is unknown when the order is sent) gives the same plateau ($392 / 1,603 / 1,539 / 1,504 / 1,546 / 2,785 per month). **DTBP is never the binding constraint**: at $400 risk the real 4-concurrent gross exposure is median $21.7K / p90 $38.0K against ~$264K (equity ~$66K x 4), and it exceeds DTBP on only 2.4% of entries at $2,000 risk — liquidity binds first by an order of magnitude. Cost fragility is larger than the capacity finding: at **1.5x** the spread curve TRAIN hold falls +0.0510 -> **+0.0346** and at **2.0x** -> **+0.0182** (weekly R 1.19 -> 0.81 -> 0.43), and the 2R exit goes **negative at 2x** (+0.0273 -> −0.0066) — and the thin names this book lives in are the ones likeliest to quote wider than the curve. TEST, read here only for the liquidity distribution, is negative on both exits (hold **−0.0827** t −1.33, 2R −0.0484 t −0.97), so every $ figure above is a TRAIN/VAL-conditional arithmetic, not a forward expectation.
- NEXT: nothing is enabled and no filter is proposed — this stage made **no selection**, so none of its 64 cells charges a decision; but if the liquid sub-book is ever pursued, its 10 band cells count toward that filter's denominator. Two things worth carrying: (1) at the $100 risk the F5 engine runs today, F6 is a **$400-500/month, ~20-trade-a-week** instrument — sized for live evidence, not for income, and the owner should hear that number before any F6 dry run is framed as a P&L path; (2) the capacity and the edge are **anti-correlated by band** ($20-50 price: +0.087R and $461 median capacity; 2M-10M ADV: +0.076R and $928 — the only two cells where both are decent, on 21-26% of the book), so the right question for H/F6 is not "which losers to veto" but "does the edge survive restriction to the half of the population that can be sized", which is a DIFFERENT pre-registration from the loser-veto grid H/F6 just spent and must be era-split (both TRAIN halves + VAL) before it is believed — and H/F6's own finding that two filters passed every TRAIN test and still INVERTED on VAL applies here in full.
## 2026-09-17 — Stage H / F5 (the live HOD-break book) — written by the coordinator from the agent's result
- RAN: H/F5/x0..x10 — parity exact vs C; three populations (DECL / LIVE-C / LIVE-S); 120 feature buckets with H1/H2; 12 mechanism vetoes; greedy ≤3 stack frozen in FREEZE.md; VAL once.
- SAYS: both stacks FAIL VAL. Live gates ($20 floor + spread/R ≤ 0.15) worth +0.04R and cost 94% of trades; 2R stack −0.141 → +0.010 TRAIN but −0.210 → −0.196 VAL (32% green); hold stack +0.154 TRAIN → +0.003 VAL, tail-dead. No ORB-shaped sub-population. 311 cells; MDE 0.189R.
- NEXT: no engine gate; HOD stays dry. V4 (10:00–11:30) / V5 (cum $ < $1M) / V1 (PDR < 5%) are the best-evidenced cuts if ever revisited. Money moves to F6-PDR (H/F6/f6_pdr_book.md, ENGINE_SPEC.md).

## 2026-09-17 — Stage H / F6_rebuild: independent reimplementation of the F6 red-to-green book from prose only
- RAN: `H/F6_rebuild/{prefilter,scan,book,monthly,obtain_check,open_bar_signal}.py` — own code, written from the prose spec alone (no `fuckup_audit` file outside the new directory read, no `bf_zero/build_candidates*.py`, `B/build_candidates4.py` or `B/score5.py` read). Funnel: 647,796 universe rows -> 396,210 at open>=5 -> 140,144 at prior-day range>=8 -> 67,178 red-open symbol-days with a tape -> 42,522 signals -> **7,606 candidates** (30,256 killed by the range-so-far floor, 3,068 by the 0.6% cap, 1,436 after 14:01) -> booked 12/day x 4 concurrent: **2,011 hold / 2,214 2R / 2,038 partial**. Four spec variants run (signal from 09:31 vs 09:30-inclusive x stop incl/excl the signal bar), all three exits each, plus the obtainability audit re-reading the tape. `H/F6_rebuild/REPORT.md`, `trades_*_{ai,ax,bi,bx}.csv`, `stats_*.csv`, `monthly_ai.csv`, `obtainability_ai.csv`. Nothing outside the directory written; no config, service, cache or order touched.
- SAYS: primary variant (09:31 start, stop inclusive), mean net R / t — **hold TRAIN +0.0626 (1.51) / VAL +0.2066 (2.51) / TEST −0.0420 (−0.63); 2R +0.0726 (2.45) / +0.0764 (1.80) / −0.0220 (−0.42); partial +0.0783 (2.30) / +0.1147 (2.04) / −0.0163 (−0.28)** at 21–29 trades/week, WR 40–50%, stop rate 26–34%, costs 0.030–0.033 R/trade. **TEST is negative on all three exits**, and VAL's hold number is carried by two months (2026-04/05 = +74.4 of +109.7 R). The two ambiguous switches are not cosmetic: allowing the 09:30 bar to be the signal (it NEVER adds a trade — the range floor has no prior bars, so those 13.6% of red-open days are deleted) moves TRAIN +0.07 R and VAL −0.13 R, i.e. the splits disagree on the sign of the switch; the stop window moves <= 0.03 R. Obtainability is clean in the direction that matters: entry fills 2011/2011 inside their bar and 2011/2011 the bar immediately after the signal bar, eod fills 100% at the first bar >= 15:55, **zero** target fills above what the bar offered; the two failures are both CONSERVATIVE — 37.3% of stop fills land below the exit bar's low (the 0.999 slip on a bar that opens at its low) and 29% of target legs sell into a bar that gapped entirely above the target. **28.5% of on-time signals are lost to the 0.6% fill cap.** Structural finding for the reader: with the prose's source precedence (`bars_sip.db` first, `data/cache.db` only when the key is absent) **75% of booked trades come from `cache.db`, not the SIP tape** — the precedence choice, not just the rules, selects the book.
- NEXT: nothing enabled, nothing proposed; this stage made no selection and adds no filter. The comparison against the first implementation is the reader's to make — three numbers to compare first are the candidate count (7,606 pre-book), the booked counts (2,011/2,214/2,038) and the TEST sign; if they differ, the ordered suspects are (1) bar-source precedence, (2) whether the red open is read off the daily file or the 09:30 bar (0.56% of symbol-days disagree), (3) whether "next bar" means the next clock minute or the next bar that exists (14.4% of candidates differ).

## 2026-09-17 — Stage I (owner: "stack multiple strategies to maximize P&L") — the stacked book + the portfolio of sleeves
- RAN: `I/PREREG.md` FIRST (8 declared cells = 4 stacks S0 F6 / S1 +F14 / S2 +F11 / S3 +F8 N=30, x 2 exits, TRAIN+VAL; one population for all four families — `C/pop_c.csv`, price>=5, R>=1%, entry<=14:01, `range_so_far_pct>=5` and **`prev_day_range_pct>=8`** transferred unchanged from `H/F6/f6_pdr_book.py`; dedupe one trade per symbol-day with declared priority F6>F14>F11>F8N30; contract (c) and `run_book(12,4)` identical to the F6-PDR book; freeze rule = highest VAL weekly R, TEST after), then `I/i_extract.py` (45,764 signals), `icore.py`, `i_score.py` (**S0 reproduces `f6_pdr_book.md` EXACTLY on all four published cells** — hold TRAIN 1122/+0.078/t 1.81, hold VAL 519/+0.251/t 3.28, 2r 1217/+0.058/2.00, 593/+0.110/2.56), `FREEZE.md` + its amendment written BEFORE the TEST run, `i_test.py` (TEST once), `i_bars.py` (fill-bar + trailing-5-min dollar volume for all 45,764 rows, `H/F6_sizing/f6_bars.py`'s source precedence cache.db->bars_sip.db, **coverage 100.00%, fill inside its own bar 100.00%**), `i_cap.py` (the 1%-participation capped twin at $150/$300/$600 + the live-computable prior-4-bars form + spread x1.5), `i_portfolio.py` -> `I/REPORT.md`, `frozen_stack_trades.csv` (2,017), `frozen_capped_trades_300.csv` (1,412), `a_cells.csv`, `a_test_cells.csv`, `cap_cells.csv`, `b_portfolio_months.csv`. No config, service, cache or order touched.
- SAYS: **stacking is strictly dominated, monotonically, in all three splits — the freeze rule picked F6 ALONE.** Weekly R (hold) S0 **+1.65 / +5.91 / +2.79** (TRAIN/VAL/TEST) vs S1 +1.67/+5.71/+2.36, S2 +1.07/+5.04/−1.37, S3 +0.54/+5.01/−2.47, while TRAIN worst week goes −10.7 -> −13.9 and weekly MDD −24.3 -> −28.5 and months green 10/12 -> 5/12: more trades, less money, a bigger hole. One of 16 TRAIN/VAL cells beats S0 on its own split (S1 hold TRAIN by +0.02 wkR) and it loses on VAL and TEST. **The mechanism is measured, not inferred**: the stack DOES add frequency — 92% of S3's 729 new TRAIN hold trades land at a minute when the F6-alone book had a free slot, so the 12/4 budget was never binding — but those added trades are **−32.9 R** while the 492 F6 trades they crowd out later in the day are **+38.1 R**; per family, F11's own booked trades are −0.14 R in every split x exit and F8 N=30's are ~0 on 599 of 1,359 TRAIN trades, F14 alone is positive on TRAIN (+0.062/+0.097) and still a wash on the book. Power: unpaired TEST MDE is **0.43 R/trade** (per-trade SD 2.73) so no single split ranks anything — the finding is the sign, S0 > S3 in 5 of 5 split x exit comparisons (sign p 0.031) plus the direct decomposition. **The stage's costliest number is the capacity twin**: at $300 risk with a 1%-of-5-minute-tape participation cap, the frozen book keeps 49.5% of the population and goes +0.073 TRAIN / **+0.075 VAL (from +0.251) / −0.146 TEST (from +0.104)**, i.e. $1,274 / $1,877 / **−$3,229** per month (live-computable prior-4-bar form: $937 / $1,905 / −$2,722) — the cap that makes the book obtainable deletes the illiquid names that carry the R; it is not a haircut, it is a different book. Tails unchanged: top-5% of trades = 296% / 114% / 305% of total R. **Portfolio (Part B, 2025-01..2026-09, each sleeve at its own declared sizing, no re-simulation)**: intraday capped $300 +$11,765 (+$560/mo, 14/21 green, worst −$6,468) + QQQ noise $60K 1x +$4,503 (+$214/mo, 13/21, worst −$2,243) + ORB B+ $10K stage +$7,187 (+$342/mo, 15/21, worst −$223) = **+$23,455, +$1,117/month mean, median +$1,443, green 11 of 21 months, worst month −$7,400 (2026-07), monthly-curve MDD −$13,149; 2025 +$21,356, 2026-YTD +$2,099**. Monthly correlations +0.15..+0.21, sigma of the sum 0.75x the sum of the sigmas — diversification is real, but the combined hit rate (11/21) is LOWER than any single sleeve because the intraday sleeve is ~5x the others and decides the sign. TQQQ 1x is **not** a fourth sleeve (rho +0.99 with QQQ — leverage on one sleeve; it is why "sum of 4" has a −$12,184 month). 84 cell-instances; TEST read once (4 cells, 3 of them by a pre-written amendment, descriptive only).
- NEXT: nothing is enabled and nothing should be. Three things to carry: (1) **stop stacking correlated shapes** — F6/F11/F14/F8 are all one-minute long breakout/reclaim entries on one population, so pooling them is adding noise at a budget that was never binding; the only stacking evidence that points anywhere is Part B's +0.15-0.21 CROSS-SLEEVE correlations, i.e. different horizons/instruments/directions, not more families; (2) **every future $ number for F6 must be the capped one** — the uncapped VAL $7,802/month is unobtainable and the capped TEST is −$3,229/month, so the honest sentence is "$500-600 a month at $300 risk on 2025+VAL, negative on TEST"; (3) live-ability is the binding constraint on the whole portfolio right now — **zero of the three sleeves can trade today** (intraday has no engine and no dry run, QQQ needs the `Q/DRYRUN_SPEC.md` engine, ORB B+ is paused by the owner 9/14), and the only one that is a config flip away is the smallest (+$342/month, worst month −$223) but also the only one with a parity-audited engine and the shallowest drawdown of the three.

## 2026-09-17 — U3 fetch: the liquid universe's 1-min SIP tape (data job, nothing scanned or scored)
- RAN: `U3/build_keys.py` (U3 = `E/u3_keys.csv`, 1,512,031 symbol-days / 5,571 symbols / **2025-01-17..2026-09-04, 410 days** — not the 2025-01-02..2026-09-11 of Stage E's prose, which is the daily panel's span; minus the 84,416 keys `E/bars_causal_index.csv` already attempted, minus the 205,401 keys `research/bf_zero/bars_sip.db` actually holds bars for via `select distinct symbol from bars where day=?` per day -> **1,222,214 to fetch**), then launched `U3/fetch_u3.py` DETACHED at 13:50 UTC (one process, `nice -n 10`, `ulimit -v 1300000`, `ARROW_DEFAULT_MEMORY_POOL=system`, >= 1 s between Alpaca requests = <= 60 req/min, 429 back-off, 04:00:00-15:59:59 ET DST-exact, SIP feed, RAW adjustment) writing `U3/bars_u3/day=*/bars.parquet` (zstd-5, one row group per 100-symbol batch, `symbol`/int16 ET-minute `t`/float32 `o,h,l,c,v` — the same int16 convention as `E/bars_causal`, so the three stores read as one tape) + `U3/bars_u3_index.csv` (one row per KEY; `none`/`badsym`/`error` recorded at n_bars=0, never dropped). Then `U3/verify.py` -> `U3/verify.md`, and `U3/README.md`. Nothing outside `research/fuckup_audit/U3/` written; no config, service, cache or order touched.
- SAYS: the store is **bar-exact** — 20 keys drawn with `random.Random(20260917)` from the first 500, each re-requested ONE SYMBOL AT A TIME (neither the batching nor the parquet path reused), minute-set then OHLCV in float32: **20/20 exact, 19 of them max relative difference 0.00e+00**, the 20th (`APO-A`) rejected by the fresh REST call exactly as the fetcher recorded it (`src=badsym`). Measured rate over the first 14,000 keys **~2,900 keys/min at 2.5-2.9 KB/key** (Stage E measured 3.69 KB/key on its narrower, higher-volume population), so the full fetch is **~3.0-3.6 GB and ~6.6-7.5 h: expected done 2026-09-17 ~20:30-21:30 UTC**. Self-limiting: stops cleanly (state saved, day-in-flight dropped as incomplete) if the store passes 6 GB or filesystem free falls under 8 GB — 13.5 GB free at launch. A day is finished only after its parquet is closed, then its index rows appended, then the state saved, so a kill can never leave a duplicate or a half-day in the index.
- NEXT: **the fetch outlives this session — it is still running.** Resume/restart with the idempotent command in `U3/README.md` ("Resume command"); check with `tail -3 research/fuckup_audit/U3/fetch.log` and `pgrep -af fetch_u3.py`. Reading U3 means reading THREE stores as one tape (`U3/bars_u3/` + `E/bars_causal/` + `bf_zero/bars_sip.db`). Nothing was scanned, scored, selected or proposed here; whatever is built on this tape is a new stage under PLAN.md §1 in full — U3's membership (`dvol20_med >= $5M` and the day's `open >= $5`) is known at 09:30, which is the point of scanning it rather than the >= 5%-range-day population.

## 2026-09-17 — H/F6_reconcile: A vs B on F6-PDR, and the book under the ENGINE's own conventions (TEST read, both sides had read it)
- RAN: `H/F6_reconcile/` — `r1_extract_A.py` (A's 7,137 gated F6 rows out of `C/pop_c.csv`), `r2/r3` joins of A's and B's booked and pre-book sets on (day, symbol), `r4_prev_table.py` (both prior-day conventions for all 644,580 universe symbol-days), `pipeline.py` (ONE parameterised F6 spec with every disputed convention as a switch), `r5_causes.py` (all 734 TEST candidate divergences re-derived from raw bars under both conventions + single-switch attribution), `r6_stores.py`, `r7_shared.py` (56/56 differing shared TEST trades re-derived), `r8/r9` (implementation **E** = `trading/red_to_green.py` + `hod_break.{entry_fill,walk_exit,run_book}` over 273,488 PDR>=8 symbol-days, 3 exits x 3 splits + tail tests), `r10_ladder.py` (the B->E convention ladder in one walk). Read-only elsewhere, `mode=ro` on both bar stores, one `nice -n 10` process at a time.
- SAYS: **A's TEST +39.00 R (+0.104 R/trade) is +50.80 R of `ZVZZT`**, NASDAQ's test ticker (4 trades; 2026-08-18 alone +46.76 R on a $24->$126 synthetic tape) — 0 rows in `daily_bars`/`intraday_bars_1min`, so live could never trade it; ex-test-ticker A's TEST is **-11.80 R / -0.032 R/trade**, i.e. B's answer (-15.36 R / -0.042). The rest of the A/B gap is ONE defect: `B/build_candidates4.py:99` sets `BFZ_SLIP=0`, so **A's level is the prior close exactly** on 7,137/7,137 rows, not the declared `x 1.003` — that switch alone explains 308/339 A-only and 309/395 B-only TEST candidates and 54/56 differing shared trades. Bar-store precedence is a NON-issue (cache.db and bars_sip.db are **disjoint**: 1,458 / 535 / **0 both** on TEST keys, and both ARE the Alpaca SIP tape); prior-day OHLC universe.csv vs the Databento panel is identical on all 644,580 keys; "next bar" and "14:01 on the fill bar" are not A/B differences at all. Decisive: **`red_to_green.detect` SKIPS floor-failing bars and keeps scanning, while A and B both take the first level break and kill the day** — the ladder (L0 reproduces B to 3 decimals: 1114/531/366 trades, +0.0623/+0.2065/-0.0421) shows that one convention takes hold from **+0.067/+0.215/-0.046 to -0.027/-0.012/-0.102**. **Implementation E is negative on every split and every exit** (hold -0.027/-0.012/-0.102, 2R -0.039/-0.061/-0.098, partial -0.038/-0.050/-0.081; 9/21, 4/21, 7/21 months green) and every cell gets MORE negative with the top 1%/5% removed or winners capped at +3R. Full tables: `H/F6_reconcile/REPORT.md`.
- NEXT: **`config.yaml:297` already carries `red_to_green: enabled true / dry_run true`, armed for the 2026-09-18 12:30 UTC boot, and its comment quotes A's six numbers as justification** — all six are the artefact. Nothing is at risk today (dry_run places zero orders, BF/ORB paused), but the dry run will print ~2x the study's signals at an expected **-0.03..-0.10 R/trade**, so the comment must be corrected and `dry_run: false` must not be flipped until either the engine's `detect` is changed to the first-break-then-floor rule AND re-validated (still -0.042 R on TEST) or the book is dropped. Standing fix for every study on this population: exclude `^Z[A-Z]ZZT$` — `research/bf_zero/universe.csv` carries 391 test-ticker symbol-days (ZVZZT 389, ZJZZT 1, ZXZZT 1).

## 2026-09-17 — Stage J (the liquid universe U3): builder ready, scan running
- RAN: `J/PREREG.md` + its **ADDENDUM 1-3** (written 14:40 UTC, BEFORE the scan; the 19 days built at 14:20-14:27 under the un-amended spec were DELETED and the scan restarted from day 1), then `J/make_members_u3.py` (1,511,915 U3 symbol-days / 5,568 symbols / 410 days, `univ_flag`), `J/build_candidates_u3.py` (long families imported from `B/build_candidates4.py`, shorts from `G/build_candidates_short.py`; THREE bar stores read as ONE tape — `U3/bars_u3/` parquet, `E/bars_causal/` parquet, `bf_zero/bars_sip.db`; engine conventions from `H/F6_reconcile/pipeline.py::E_CFG` — signal from 09:31, the 14:00 cut on the SIGNAL minute, running low THROUGH the signal bar for the running-stop families, next bar = the next bar the tape PRINTS; ONE fill model = next printed bar's open under the 0.6% cap; three exits hold / 2R / 50%-at-+2R-then-breakeven; per-row trailing-5-minute dollar volume for capacity; spread from the cost curve's LIQUIDITY rows, the small-cap price-band number kept beside it as `spread_pb_bps`), `J/verify_rows.py` (hand recomputation from prose + obtainability + Stage-E parity), `J/score_u3.py` (the 144 declared cells), `J/README.md` with every resume command. Writes only under `research/fuckup_audit/J/`; `data/cache.db` read once, read-only, for the ADDENDUM-1 symbol list.
- SAYS: nothing yet — **this milestone is the builder, not a result.** Two things already found and fixed before any number existed: (1) the fill-acceptance test was INVERTED in the first draft (`stop <= entry` rejected instead of accepted for longs), which held the fill rate at 0.5% and produced negative `r_pct` on every row — caught by the 5-row hand recomputation, not by review; (2) the three coordinator corrections — **test tickers** `^Z[A-Z]ZZT$` (ZAZZT/ZJZZT/ZVZZT, 116 symbol-days) dropped outright and the **309 symbols / 29,775 symbol-days (1.97%) with zero rows in `data/cache.db::daily_bars`** tagged and excluded from every scored cell (tagged, not deleted, because 309 of the 312 are real delisted/acquired names and preferred classes — excluding them is a survivorship filter in the opposite direction, so its size is printed); **F6's level buffer** corrected from the inherited `x1.000` (every pass-1 builder pins `BFZ_SLIP=0`) to the declared engine `x1.003`, with every other family's buffer now stated in a table (and the finding that the long side's F6 and the short side's S3 were INCONSISTENT — S3 already carried its 0.997 mirror); **both scan rules** (`first` = the first break is the signal, `keep` = keep scanning later breaks of the same level until one fills, `trading/red_to_green.detect`'s rule and the only one that could go live) emitted as twin cells for F6/F8/F14/F11, named on every table. Measured on 2025-01-17: 17,465 signal rows, 98.4% filled, the `keep` twin adds 103 rows (0.6%). Build rate 29 s/day, ~7 MB/day, so ~2.8 GB and ~3.3 h of CPU for 410 days — it CHASES the U3 fetch (ETA ~21:00 UTC) and idles between days.
- NEXT: the U3 fetch and the Stage-J builder both **outlive this session**; every resume command is in `J/README.md` (fetch, members, builder chase mode, verifier, scorer, in that order). Nothing is scored, selected, proposed or enabled. TEST is unread and stays unread until a freeze is written. The honest expectation to hold: this is the first scan of the liquid universe, and the only pre-registered question is whether ANY of the 144 cells clears G1/G2 at a capacity that carries $1,000 of risk — Stage I's finding was that the small-cap book's money lives in names that cannot carry the money.

## 2026-09-17 — D1 / ORB: the B+ vetoes retro-applied to the live trades, and the slot dose-response on the honest fills
- RAN: `D1_orb/taskA_retro_veto.py` + `taskA_rank.py` (the SHIPPED helpers `trading/orb_{pdr,g1,range_size,catalyst}_veto.py` + `study_orb_filter`/`study_orb_sizing` with the values in `orb.yaml`, applied to the 117 ORB rows of `live_trades_dump.csv` and their `pattern_data`; features row for 113/117, the other 4 rebuilt from `cache.db daily_bars` read-only — `range_size_pct` NOT reconstructible there), then ONE full bar-walk of `study_orb_pipeline_static_lock.py` (`ORB_BT_DUMP_CANDIDATES`, 13,033 candidates, shipped exit physics) that **reproduces the nightly production book to the cent — $7,186.64 / 88 picks / 70 fills**, then **14 selector-only replays** off that dump (`ORB_BT_RESIM_CACHE`) at slots 3/4/6/8/12 × Q1 on/off + 4 pool-widening cells, with `ORB_BT_ACCOUNT = 3,333.33 × N` so every position is sized identically and only the slot count moves. `D1_orb/REPORT.md` + `taskA_live_retro_veto.csv`, `taskB_summary.csv`, `taskB_monthly.csv`, `capacity.py`, `pool_vs_threshold.py`. Nothing outside `research/fuckup_audit/D1_orb/` written; no config, service, cache, cron or order touched; no veto threshold, z-param, cutoff or mult re-tuned.
- SAYS: **the review's pre-registered bar is met — 69.8% of live ORB stop dollars (41 of 55 stops, −$8,525 of −$12,214) sit in trades the B+ stack vetoes; 94 of the 117 live trades are blocked (−$2,428).** The 23 survivors are −$2,375, but **−$1,959 of that is two `stop_loss_market_fallback` exits** (EHGO 6/25, IRE 6/29), so **survivors ex-execution-pathology = 21 trades, −$416, −2.42R, WR 47.6%**; the live loss decomposes −$2,428 dead-config selection / −$1,959 execution / −$416 residual. Tighter: only **8 of 117 live trades are both inside B+'s top-3 for their day and clear every veto, and those 8 realized +$363** — the 116-loser dive is a post-mortem of a machine that would have owned 8 of them. Subtractive only: the B+ frozen fit moves 26 of 113 live trades to a different quintile, so it cannot price the trades it would have taken instead. **Dose-response: the selection carries edge below rank 4 and dies by rank 9** — marginal R per added pick **+0.725 (slot 4) / +0.456 (5–6) / +0.293 (7–8) / +0.042 (9–12)**, and $/month at identical per-position sizing **$342 (3, shipped) → $486 (4) → $597 (6) → $687 (8) → $741 (12)** with MDD −714/−813/−782/**−576**/−944 and red months 6/2/3/2/4: going 3→8 slots roughly doubles the dollars AND shrinks the drawdown, going past 8 buys $54/month for a 64% worse drawdown. Two structural findings the ship config hides: **the $3,333 per-position cap binds on 100% of picks, so realized risk is ~$143 and `risk_per_trade_usd: 375` is an inert knob**, and the declared budget is not the capital needed (max 2 picks in one day at N=3, 5 at N=12 → $6.7K/$16.7K really deployed). **Widening the pool is not available and does not work**: with Q1 dropped the composite threshold is inert, the whole candidate universe is only 2.26× the B+ pool (3× does not exist), and at N=12 the 2.03× pool buys +$852 for MDD −$944→−$2,388 and worst month −$411→−$1,193. Participation from `range_total_volume × entry_price`: at 3× the per-position cap the median pick takes 0.96% of the 5-minute range's dollar volume, p90 5.04%, 13 of 118 picks over 5% — so **ORB B+ at 8 slots and 3× sizing is a ~$2,000/month book on ~$40K of buying power**, and at the shipped 3 slots and stage sizing it is $342/month. 14 runs, 108 reported cells, 0 thresholds tuned.
- NEXT: nothing enabled, nothing flipped — ORB is still `strategy.enabled: false`. Two decisions are now evidence-backed and both are the owner's: (1) **resume B+ at the $10K stage** (the live loss is a config-era + execution artefact, not the shipped book), and (2) if it resumes, **`max_concurrent: 3 → 8` with `account_budget_usd: 10000 → 26667`** is the only slot change the data supports (more dollars, smaller drawdown, 6→2 red months, per-position sizing unchanged) — it is one yaml edit of two lines plus the three kill rails, and it needs the independent check of CLAUDE.md §"No research claim ships" before it goes in front of the owner as a proposal. Do NOT read the N=8 Q1-off $18,300 as an argument for dropping the Q1 filter: +$4,307 of it is one 5-month split. The −$1,959 of `stop_loss_market_fallback` inside B+'s own surviving picks is a Day-3 engine bug, not a selection question, and it is larger than the residual selection loss.


## 2026-09-17 — Stage D3_exec (execution post-mortem on the 22 real pathological live exits)
- RAN: `D3_exec/d3_walk.py` (22 rows from `data/trades.db` ro with exit_reason in market_fallback/timeout/bracket_sl_race/never_reconciled/thin_liquidity_reject/unknown_exit, each reconstructed minute by minute against `data/cache.db` 1-min bars) and `D3_exec/d3_quant.py` (four benchmarks per event — clean stop ×0.999, trigger, the bid the engine had in hand, its own limit — plus qty/bid_size, tape participation, and counterfactual C3 with an obtainability check) -> `events_walk.txt`, `events.csv`, `quant.csv`, `REPORT.md`. Journal is vacuumed to 09-09, so only HCAI 09-11 has a live trace (`hcai_journal.txt`) — it confirms the mechanism the other 21 are read off telemetry. READ-ONLY; no trading/ code, config, service, order or cron touched.
- SAYS: **the real execution cost is −$1,352.40 (BF −$809 = 45% of the BF book's entire net P&L; ORB −$537 = 11% of its loss), not the −$5.0K / −$1,959 previously reported** — both prior figures counted the trades' FULL P&L including planned stop risk (EEIQ's excess over a clean stop is −$161 of its −$3,738; EHGO+IRE −$438 of their −$1,959, so D1_orb's "survivors ex-fallback −$416" should read **−$1,937 / 23 trades**). Eight mechanisms, all in `stop_monitor.py`: the bulk-cancel kills the broker SL leg **before** our own limit (23–69 s measured naked window); the limit prices the WHOLE qty at bid−0.30×spread when the order was **2.1× to 46.9× the displayed bid in 9 of 11 events**; `_poll_order_fill` refuses `partially_filled` as success so a top-of-book fill is judged a total failure and the rest is market-ordered into the hole it just made (RBNE 14.2× the bid and 47% of the minute's volume → −$305, −1.30R); `_on_trade` has **no** SIP condition filter, and RCAT 06-29 was liquidated on a 9.11 print while the quote was 9.78/9.79 and the bar low 9.7203; the March `quote_tight` tier put EEIQ's sell limit at the MIDPOINT, above the bid; the escalation branch **overwrites the exit reason**, so `stop_loss_market_fallback` currently holds a +$122.84 trail winner (FJET), an EOD `stage_force_flat` (HCAI) and three real stops — every `exit_reason` grouping in this tree is wrong; four `unknown_exit` rows book `pnl = 0.00` with `exit_price = fill_price` hiding **−$1.7K..−$6.8K** (NPT alone −$4,818 by the tape); four `never_reconciled` rows were written once at submit and never given a terminal state. Counterfactual C3 (immediate marketable limit at bid − max(1¢, 0.25×spread), obtainability checked on the submit-minute bar): **+$1,081 gross, +$797 decidable** (BF +$594, ORB +$485), EHGO's +$285 undecidable because the ORB rows carry no `exit_submitted_at`.
- NEXT: eight fixes are specified with code locations, config keys, 14 unit + 4 integration tests and `enabled:false` byte-identical rollback contracts — FIX 1 (replace the broker SL leg, never cancel it: the HOD engine's design, `replace_order_stop_price` already exists) and FIX 6 (split `exit_branch` from `exit_reason`) are the two with no measurable $ that everything else depends on; FIX 2/3 (slice to the displayed bid, re-price via `replace_order_limit_price`, accept partials) carry the +$797. Ship order: 6 → 7 → 1 → 3 → 2 → 5 → 4 → 8. Live metric to judge on: `slip_vs_bid_bps`, today median −58 / worst −229, target median ≥ −25.

## 2026-09-17 — Stage D5_r2g (by-hand loser dive on the red-to-green FIRST-BREAK book)
- RAN: `D5_r2g/facts.py` (the 2,011 booked HOLD trades of `H/F6_rebuild/trades_hold_ai.csv` = implementation B, the first-break rule, minus 2 `^Z[A-Z]ZZT$` rows → **2,009 trades / 417 days**, each re-read minute by minute off `research/bf_zero/bars_sip.db` with `data/cache.db intraday_bars_1min` as the per-trade fallback the book's own `src` column names, SPY/IWM from `research/lit_review_2026/etf_1min.db`, daily context from `prefilter_all.csv`/`daily_bars` ro), `analyze.py` (25 worst + 25 best days written out trade by trade; eight qualitative classes with a precedence declared before the run — entry-knowable first; winner-vs-loser entry-fact quartiles; 10 worst weeks; sequence and hour), `extras.py` + the tail pass -> `REPORT.md`, `trades_classed.csv` (the per-trade CSV with `cls` + seven non-exclusive `f_*` flags), `trades_facts.csv`, `days_facts.csv`, `cells.txt`. Read-only everywhere; one `nice -n 10` process under `ulimit -v 1300000`; nothing outside `D5_r2g/` written.
- SAYS: **the two loser classes that are decidable at the fill are the wrong ones to cut, and the book is its own top 5%.** (d) spread/thin owns **39.3% of all loser R (−341.5 of −868.3)** and 41.5% of the winners (+454.7) — the thin cohort is **net +95.7 R on 808 trades**, so dropping it takes TRAIN +0.062→+0.028 R/trade ($1,725→$433 a month at $300 risk); (e) late/extended is net +3.3 R on 159 trades. Dropping both: TRAIN +0.045 / VAL +0.177 / TEST −0.071 — worse than as-booked in two of three splits. The near-perfect separators are post-entry only: **(a) level failure 178 losers −168.1 R vs 5 winners +1.2 R** and **(g) slow bleed 445 losers −302.2 R vs 43 winners +3.5 R** — a time-stop argument, not an entry filter, and not run here. Of twelve entry facts, only the index at the entry minute clears the declared ≥20%-same-direction bar in TRAIN and VAL, and its absolute size is **1.4 bp of SPY** (the relative test is degenerate on a median of ~0). The one structure holding its sign in all three splits is the **entry ordinal** (1st entry +0.164/+0.681/+0.148, monotone decay to +0.038/+0.033/−0.067 at the 4th; not time-of-day — it survives inside the 09:xx hour alone), and `seq<=2` flips TEST from −15.4 R to +13.1 R while *lowering* the TRAIN book from $1,725 to $1,310 a month. **The tail settles it: every rule × every split is negative ex-top-5%** (as booked −0.156/−0.116/−0.248; seq≤2 −0.196/+0.002/−0.174; seq≤2 & stop≥3% −0.158/−0.057/−0.133), and winners capped at +3 R put TRAIN as-booked at −0.005. Weekly book R is **+0.36 correlated with the SPY intraday sum, +0.40 with IWM** (7 of the 10 worst weeks were negative-SPY weeks) — this book is long beta on top of being tail-carried. **353 cells**; TEST read once at the end for the single combination VAL agreed with TRAIN about.
- NEXT: nothing proposed, nothing enabled. The frame matters more than any cell here — this is the FIRST-BREAK book, **not** the rule `trading/red_to_green.py` runs, whose own book is −0.027 / −0.012 / −0.102 R (`H/F6_reconcile` §3); so `config.yaml`'s armed `red_to_green` block must stay `dry_run: true` and its comment (which still quotes implementation A's ZVZZT-inflated numbers) still needs correcting. The one thing this dive hands forward is the size of the **slow-bleed / level-failure classes** — the same shape the ORB time-stop work found — as the only lever with real mass, to be tested as a time stop on a re-walk, not inferred from these tables.

## 2026-09-17 — Stage D3_exec SHIPPED (six of the eight fixes, TDD, nothing deployed)
- RAN: `D3_exec/REPORT.md`'s ship order 6 → 7 → 1 → 3 → 2 → 5, one commit per fix on `fix/spy-regime-shared-helper`, each with its tests green before the next. **FIX 6** `trades.exit_branch` (Migration 15, PRAGMA-guarded ADD COLUMN, idempotent, historic strings untouched) + `ExitBranch` + `confirmed` keyed on the branch, so a trail exit that escalates is no longer booked as a stop loss; **FIX 7** `trading/unknown_exit.py` — an unattributable exit writes NO price, NO exited_at, NO pnl and parks the row in `exit_pending_verification` (the daily resolver is a loud STUB, deliberately: it WRITES P&L and needs its own evidence); **FIX 1** `_exit_via_sl_leg` — a live bracket SL leg is REPRICED (`replace_order_stop_price`, following Alpaca's new order id) instead of cancelled to make room for our own limit, config `prefer_sl_leg_exit` default TRUE; **FIX 3** `_poll_order_fill` returns `(filled_qty, avg_price, status)`, the partial is booked, only the remainder is worked through the existing `_verify_fill_qty`, the event carries the qty-weighted blend; **FIX 2** the sliced re-priced exit ladder, **DEFAULT OFF** with a byte-identical-when-disabled test; **FIX 5** `urgent=True` is the default so the midpoint tier is unreachable from any exit path. `tests/fakes/fake_alpaca_broker.py` grew `replace_order_stop_price` and an opt-in `allow_close_position`. **No service restarted, no order submitted, no book re-run** — `research/fuckup_audit/D3_exec/IMPLEMENTATION.md` holds the deploy steps, the rollback table and the live metrics.
- SAYS: the five fixes that are live-on-deploy are FIX 6/7/1/3/5; the +$797 decidable counterfactual sits behind the ladder, which is dormant. **New tests: +20 in `test_stop_exit_limit_buffer.py`, +30 in `test_exit_reasons.py`, +15 in `test_stop_exit_unconfirmed.py`, +9 in `test_spread_pricing.py`, 22 new in `test_stop_exit_ladder.py`, 18 new in `tests/integration/test_stop_exit_execution.py` (NPT through the real `_sync_closed_positions`, the RBNE bracket lifecycle on a falling tape, the EHGO partial, the RBNE thin book). Whole suite 3,886 passed / 10 skipped / 0 failed.** Four existing assertions that ENCODED the defect were rewritten to the new contract (they asserted `exit_reason == 'stop_loss_market_fallback'` on an escalated exit). Two things found while building that the report did not name: the limit-submit-failure path wrote a CONFIRMED exit at `trigger_price` when its poll timed out — a fabricated fill price and P&L, the SMU/QBTZ failure mode in a second costume, now `last_resort`; and `_verify_fill_qty` used `self._alpaca` unconditionally, so closing an ORB remainder from the stop path would have been a wrong-ACCOUNT order. Note for anyone re-running the suite: it needs `ulimit -v 6000000`, not 1300000/1800000 — under 1.8 GB of address space `test_ignition_engine.py` cannot start its worker threads and reports 15 phantom failures.
- NEXT: **FIX 4 (SIP condition / NBBO sanity on the trigger print) and FIX 8 (terminal-state sweep) are NOT built** and are left out cleanly rather than half-done — both are still specified in `REPORT.md` §4 with code locations and tests; FIX 4 is worth ≈$0 in this census (RCAT's stop was hit legitimately one bar later) and is tail insurance, FIX 8 is four historic rows plus a post-close job. Deploy is the OWNER's word plus the weekend rehearsal protocol; BF and ORB are both paused, so the fixes land before the volume does. The ladder stays `enabled: false` until at least 10 real exits have been seen under FIX 1 + FIX 3 and `slip_vs_bid_bps` has been read — today median −58 bps, worst −229, target median ≥ −25.

## 2026-09-17 — Stage J: TRAIN built and scored — 0 of 144 cells clear G1 on the liquid universe
- RAN: the Stage-J chain through TRAIN. `J/build_candidates_u3.py` over 2025-01-17..2025-12-31 (236 days, 51 weeks) -> **4,859,792 signals, 97.1% filled, 3,257,389 scoreable** (9 long + 5 short family-configs, three bar stores as one tape, engine conventions, one fill = next printed bar's open under the 0.6% cap, three exits hold/2R/50%-at-+2R-then-breakeven, liquidity-band spread, per-row trailing-5-min dollar volume). `J/verify_rows.py` on the whole TRAIN file -> `J/verify_TRAIN.md`, **exit 0**: 8 seeded rows reproduced FIELD FOR FIELD by a second implementation written from prose (no numpy, no call into B4/GS/pipeline), obtainability **1.000000 on 4,719,541 fills**, and on the 16,458 F8 keys Stage E also holds **sig_m / level / stop / entry / entry_m differ on 0 rows** (F6 differs by exactly the ADDENDUM-2 x1.003 buffer on 1,874 of 1,874, and J's stop <= E's on 1,874 of 1,874). `J/score_u3.py --perm 300` -> `J/score_u3_tables_TRAIN.md`, `_results_TRAIN.csv`, `_capacity_TRAIN.csv`, `pop_j_TRAIN.parquet`, and `J/REPORT.md`. Only `research/fuckup_audit/J/` written; `data/cache.db` opened read-only once, for the ADDENDUM-1 symbol list; no config, service, cache or order touched.
- SAYS: **0 of 144 declared cells clear G1** (TRAIN mean net R > 0, t >= 2.0, >= 5 trades/week), so G2 and the pooled book were not run. Best cell **F14 N=15 / hold / R>=0.5% / PDR>=8: n 899, 17.6 tr/wk, +0.0762 R, t 1.71, MDE 0.089, WR 46.7%, 53% weeks green, worst week -12.7 R — and ex-top-5% it is -0.1255**, i.e. tail-dependent and one of 144. Search-adjusted permutation (300 day-level sign-flip draws over all 144 booked cells): **observed max TRAIN t 1.71 vs a null 95th percentile of 4.52, p = 1.000** — with 144 cells the significance bar is t > 4.5, not 2.0. **Two structural findings that do not depend on any cell.** (1) PREREG's cost premise is FALSIFIED in the direction that raises the hurdle: the spread is right (> $50M/d ~17 bps, $5-50M/d ~52 bps) but the assumed 2% stop is not — median R on liquid names is **1.24% of price**, so the median round trip costs **0.143 R, not 0.05 R** (p90 0.36 R). The gradient is visible end to end: pre-book mean net R **-0.098 for > $50M/d vs -0.237 for $5-50M/d**, and 09:30-09:35 is the worst hour at **-0.311** vs -0.067 after 13:00. (2) **Capacity fails before edge does, for a reason specific to liquid names**: a tight stop turns fixed dollar risk into large notional ($300 risk / $0.58 per share / $43 stock = a $22.5K order against a median trailing-5-minute tape of $515K), so at the declared 1% participation cap **76.2% of the population is untradeable at $300 of risk, 89.7% at $1,000, 94.2% at $2,000**; the only family that escapes it is F9 gap-and-go (0.46% at $300, because a gapper's stop is wide) and F9 is the WORST long family pre-book (-0.290 R) — the two properties this book needs are anti-correlated on this universe. Amendments: A1 dropped ZAZZT/ZJZZT/ZVZZT (116 symbol-days, 0 reached the file) and excluded 309 `no_daily_bars` symbols whose effect is measured at **0.0005 R and is not load-bearing either way**; A2 corrected F6's level buffer from the inherited x1.000 (every pass-1 builder pins `BFZ_SLIP=0`) to the declared **x1.003**, and found as a by-product that **the long and short sides of this tree were inconsistent** — G's S3 already carried its x0.997 mirror explicitly while F6 carried nothing; A3's `keep` scan rule adds **1.64% of trades** (they are better than the base, -0.091 vs -0.169) and changes no verdict, because the first-scan fill rate on liquid names is already 97.1%. Phrasing: **no edge was detectable in the liquid universe U3, at a 1-minute horizon, in a 12-a-day 4-concurrent book, over 2025-01-17..2025-12-31, at the measured liquidity-band spread — and the smallest per-trade mean this test could have called significant at t >= 2 is 0.050-0.099 R.** Three defects the process caught and fixed before any number shipped: an INVERTED fill-acceptance test in the builder's first draft (caught by the hand recomputation, not by review), 663 phantom "tape differences" that were this stage's own `%.8g` CSV format against an absolute tolerance, and an `iterrows()` reservoir that blew the verifier's ulimit.
- NEXT: **the U3 fetch and the Stage-J builder are still running and outlive this session** (fetch at day ~250/410, ETA ~21:30-22:00 UTC; builder chasing it at 2026-01-14). Every resume command is in `J/README.md`. VAL and TEST are NOT built; **TEST is unread and stays unread until a freeze is written**. When the build finishes: re-run `verify_rows.py` on the full file, then `score_u3.py --perm 500`. One warning for whoever reads VAL: **G1 selected ZERO cells**, so a good-looking VAL cell is not a confirmation of anything — it is a fresh look at 144 cells and must be counted as one. Nothing is enabled, nothing is proposed, no engine work was started, and the NBBO pull PREREG makes conditional on a G1 pass was not made.

## 2026-09-17 — Stage K (multi-day holds on the liquid daily panel, PREREG 2026-09-17)
- RAN: `K/step0_pricescale.py` (200 keys panel vs `cache.db::daily_bars` -> the Databento daily panel is **RAW and matches Alpaca to float32**, 97.5% inside 0.01%; it also found **169,341 zero-price panel rows (3.4%)** which in the first pass made `prev_close=0` -> `inf` gaps that passed the K1 cut and poisoned every later cumsum window — fixed before any number was produced); `K/build_k.py A` (the 5 declared families on the 420-day panel, universe = dvol20_med >= $10M + close >= $5 + common stock by the asset-class map + test tickers out + early closes not signal days -> 1,254,449 base / 919,910 primary symbol-days; entry at the next OPEN, exit at the hold's last CLOSE or the first close breaching the stop; PREREG cost band + two sensitivities; 10/20-slot first-come book; 5,000-draw sign-flip permutation across the 20 cells) -> `K/phaseA.md`, `K/cells_trainval.csv`, `K/trades/*.csv` (25 cells, 55,180 trades); `K/diagnostics.py` + `K/diagnostics2.py` (D1 random-market control, D2 book layer, D3 the M41 reconciliation, D5 the ranking-key decomposition); `K/report_k.py`; `K/FREEZE.md`; `K/REPORT.md`.
- SAYS: **0 of 20 pre-registered cells pass G1** (best TRAIN `K3_h3_n10` +17.4 bps net/trade at **t +0.51**, search-adjusted p 0.945; best VAL `K2_h10_n20` +166.4 bps but TRAIN −23.0 and **−17.9 with its top 1% removed**), so G2 was unreachable and **TEST was NOT read** (freeze written first). Smallest visible effect: 13.4–171.3 bps/trade on TRAIN, 22.7–314.1 on VAL. **Capacity is the one PREREG claim that survives** — $415K–$611K per position = a $4.1M–$12.2M book at 10–20 slots — with nothing profitable to put in it (best cell +1.07%/month, worst −14.3%/month). The null is NOT the scorer: a random book on the same universe/fills/costs reproduces market drift (+5 bps/day vs SPY's +6.7), and an **independent rebuild from prose reproduced K1 exactly** (261/261 trades, −122.620 bps, t −1.432); K3 differs only at the decile boundary and fails the gate under both readings. Three real findings: (1) the declared ranking key is **worse than a coin flip** — swapping in a random draw improves K4 on both splits (−80.1/−62.4 -> −55.2/−48.0) and K5 likewise, i.e. signal intensity is negatively selective in a slot-bound book; (2) K2's whole signal set is +23.8/+64.1 bps net but its 10-slot declared-key cell is −78.8/+19.2 — the book, not the family, is the loss; (3) against the random control at the same hold, only K2 has positive excess on both splits (+19/+47 bps per 10-day trade, t 0.79/1.44) and K1 is **83–114 bps WORSE than a random name**, the daily-scale version of the settled "gappers fade".
- NEXT: two honest gaps before this direction is closed. (a) **The declared K2 rule was never tested on TRAIN as written** — the panel starts 2025-01-02 and `high52` needs 60 prior bars, so K2 cannot fire before 2025-04-01 and no signal before 2026-01 uses a full 250-bar lookback; a real 52-week-high test needs daily bars back to 2024-01. (b) K1 fires ~6/week and its MDE is 171 bps/trade — it is untested for a positive edge, not refuted. The one mechanism worth a NEW pre-registration (with its own TRAIN/VAL/TEST, not this one's) is finding (1): rank the slot-bound book by something OTHER than signal intensity. Nothing from Stage K is promoted; 53 distinct cells were scored in total and are counted in `K/REPORT.md` §9.

## 2026-09-17 — Stage M (six exit shapes on the honest ORB book, at the live 8 slots; PREREG 2026-09-17)
- RAN: `M/exit_shapes.py` — ONE bar walk over the entered-inclusive features (`analysis_results/orb_features_20260916_2053.csv`, 13,033 candidates = 7,402 fills + 5,631 modeled non-fills, 427 days, 2025-01-02→2026-09-16) computing **all six declared exits per candidate** and writing six candidate dumps identical in every column but `pnl`/`pnl_pct`/`exit_reason`, then `M/run_grid.sh` (18 selector-only replays off those dumps via `ORB_BT_RESIM_CACHE`, 6 shapes × N ∈ {3,8,12}, `ORB_BT_ACCOUNT = 3,333.33 × N` so the per-position cap stays at the live $3,333 and only slots+exit move) and `M/run_sizing.sh` (the 2 money-line rows) -> `M/REPORT.md`, `summary.csv`, `monthly_n8.csv`, `exit_mix_n8.csv`, `exit_pnl_n8.csv`, `analysis.txt`. Shapes, declared before the run: **X0** shipped (static lock 1.75R→+0.5R, ATR floor k=0.25, 40%@+3R scale-out, touchgo, 15:45 flat) · **X1** +10-min time stop below +0.25R · **X2** +5-min time stop below 0R · **X3** no lock, hold to 15:45 · **X4** X3+10-min stop · **X5** X4+breakeven at +1R. **Three parity checks, all exact**: the parametrised walker at neutral knobs reproduces the SHIPPED `simulate_winner_stack` price AND reason on all **7,402** fills (per-row assert); X0's dump equals D1's `candidates_dump.csv` at `max|Δpnl| = 0.000000000000` over 13,033 rows; X0/N=8 reproduces D1's cell to the dollar (**$14,428.62 / 215 picks / 162 fills / MDD −$575.55**) and X0/N=3 the nightly production book ($7,186.64 / 88 / 70). Read-only outside `research/fuckup_audit/M/`; `cache.db` opened `mode=ro`; no config, service, order or cron touched; nothing re-fitted.
- SAYS: **the shipped exit wins — 0 of 5 challengers clears the pre-committed gate** (improve total AND MDD on TRAIN *and* VAL at the live N=8): X1 −2,736/−2,014 · X2 −504/−331 · X3 **+299/+180 on P&L but −260 on TRAIN drawdown** · X4 −1,916/−1,898 · X5 −3,945/−2,055. **The 10-minute time stop is the headline reversal**: it fires on 29.3% of fills and 47 of 215 picks, **wins 27 times (+$2,091) and loses 20 (−$6,253), net −$4,163** — 2025-10 alone goes $2,341 → −$44 (BNAI 1/23 −$1,259; CRCG/CRCA/CCUP all on 2025-10-02). Its earlier "lift" (`orb_timestop_validation.md`) was measured against the features CSV's OLD fixed +2R/−1R outcomes, where **winners are capped**; under the shipped uncapped exit the same cut kills the tail — and this book **is** its tail (top 5% of fills = 62% of P&L; $14,429 → **$5,524 ex-top-5%**, **$8,801 with winners capped at +3R**). "Hold the runners" tested as X3 is worth +$740 over 21 months for a TRAIN drawdown 49% deeper and red months 2→5. **Money line, X0 at N=8: $687/month at stage ($14,429 / MDD −$576 / worst month −$148 / 2 red of 21 / 44.6% of 74 weeks green) and $2,061/month at 3× ($43,286 = exactly 3×, MDD −$1,727, worst month −$444)** — participation median 0.32%→0.96% of the pick's own 5-min opening-range dollar volume, p90 1.22%→3.67%, **17 of 215 picks over 5% at 3×**, and the capital actually needed is $13,332/$40,000 (max 4 picks on one day), not the declared budget. **3× requires moving BOTH knobs**: cap $10,000 with `risk_per_trade_usd` left at $375 binds on only 36.7% of picks and yields 2.53×, not 3×. Power: the smallest per-fill mean R this stage could call significant at t=2 is **0.40 R TRAIN / 0.96 R VAL / 0.38 R TEST** — the shape differences (0.2–0.4 R) sit at that ruler's smallest tick. 20 decision cells, 0 parameters fitted.
- NEXT: nothing enabled, nothing flipped, no engine work started — `orb.yaml` is untouched and X0 is already what runs. Two things carried forward. (1) **The one honest disagreement**: ALL four time-stop shapes beat X0 on TEST (X5 $2,695 · X4 $2,229 · X1 $1,968 vs X0 $1,380) and three of them improve TEST's drawdown; that is 4 months / 38 fills against 24 months of TRAIN+VAL, the gate reads TRAIN/VAL only by pre-registration, and it is the single line to re-read if the book runs another live quarter. (2) **The adoption surface is now known and asymmetric**: X3 and X5 are *pure `orb.yaml` edits* — `stop_monitor.py:3169` already gates the lock on `lock_arm_at_r > 0`, and `lock_arm_at_r: 1.0` + `lock_stop_r: 0.0` IS a breakeven-at-+1R stop (the BT side needs one guard: `simulate_static_lock`/`simulate_winner_stack` would arm on the first bar at `lock_arm_r = 0`) — whereas a time stop is six code locations (`trading/orb_time_stop.py` as the shared spec, `orb.yaml exit.time_stop`, `orb_planner.py:42/92`, `orb_engine.py::_ingest_bars` ~1114 anchored on `pos.breakout_bar_ts` and routed through the `force_exit` seam ~1583, `exit_reasons.py` ~313, and `stop_monitor.py:1414`'s whitelist, without which the cut is silently dropped). None of it is recommended on this evidence.

## 2026-09-17 — Stage L (TRANSFER FILTERS: 8 filters whose evidence comes from ANOTHER book, on 6 near-profitable books)
- RAN: `L/PREREG.md` FIRST (6 books x 8 filters + one best-two stack per book = **54 declared cells**, thresholds frozen at the values the SOURCE book ships, decision rule TRAIN improvement >= +0.03 AND VAL improvement >= 0 AND VAL mean > 0, TEST once, 200/500-draw day-level sign-flip null, $300 risk for B1-B4/B6 and $10K-stage for B5), then `L/lcore.py` + `l1_pop.py` (the five `run_book(12,4)` populations in one schema — **parity exact**: B2 reproduces `H/F6_rebuild` VAL/TEST to 0.0000, B4 and B6 reproduce `H/F14_F8_F11` and `G/REPORT.md` to 0.0000, B3 off by -0.0091 = its 6 test-ticker rows, B1's un-booked mean reproduces `live_followthrough.md`), `l2_bars.py` (66,217 (symbol,day) tapes, **100% coverage**, bars_sip 25,797 + cache.db 40,416, the T2/T6/T7 inputs), `l3_score.py`, `l4_orb.py` (B5 = the ORB pipeline itself, filter applied to the candidate universe BEFORE ranking so the top-N selection refills; base run **reproduces the production book to the cent, 88 picks / 70 fills / $7,186.64**), `l5_test.py` -> `L/REPORT.md`, `cells_full.csv`, `perm_summary.txt`. Read-only elsewhere; no config, service, cache, cron or order touched.
- SAYS: **9 of 44 run cells passed TRAIN+VAL; on TEST 4 of the 9 improve, 3 have a positive mean, 1 survives ex-top-5%, and the whole grid is inside its own noise — observed max |TRAIN improvement| 0.2469 vs a 500-draw null 95th pct of 0.5496, p = 0.794.** The two EXIT rules are the clearest result and the transfer FAILS: T6 (10-min time stop at +0.25R, from `orb_timestop_validation.md`) and T7 (fill-minute volume < 1.5x -> sell next open, from `live_followthrough.md`) are **negative on 12 of 12 book x split pairs**, including on B5 = the very book the ORB evidence was measured on (-0.209 TRAIN / -0.643 VAL) — they fire on **65-90%** of 1-minute-book trades (vs a minority of raw ORB fills), so they do not filter the book, they replace it with a 10-minute scalp; every forced fill is a bar open inside its own bar (obtainability 100.000%, `bad_fill` 0) and was charged the STOP coefficient. **T4 (first entry of the day only, from `D5_r2g`'s entry ordinal) is the only filter whose sign transfers** — positive on VAL for 6 of 6 books, on TEST +0.190 R on B2 (mean +0.148, t 0.73, **+$753/mo vs the base's -$1,152**), +0.042 on B3, +0.077 stacked with T5 on B6 (mean +0.067, **the only passer alive ex-top-5% at +0.003**, +$225/mo) — and it buys that by cutting frequency 4-5x (B2 26.1 -> 4.9 trades/week). T1/T2/T8 are inert or already inside the books' own definitions (**9 of the 48 single-filter cells are structurally degenerate**, incl. T5 on B3/B4 where no signal before 10:00 can exist). Two disclosures in the PREREG amendments: the parity script printed the BASE books' TEST means, and the ORB pipeline prints whole-window dollar totals. One non-passing cell worth its own pre-registration: **applying the shipped range-size veto to the ORB candidate universe BEFORE ranking instead of post-selection takes B+ from 88 picks / $7,186.64 to 135 picks / $11,544.76** (fewer selected picks destroyed by the no-refill vetoes) — it fails the mean-R rule (-0.071/pick) so its TEST was not read. Smallest visible per-trade improvement: **0.066 R (B4) to 0.649 R (B5)** on TRAIN, 0.094 to 1.808 R on VAL — the B5 row cannot resolve anything and its VAL cells run n = 4 to 30.
- NEXT: nothing enabled, nothing proposed. Three things earned here: (1) **stop testing exit-side rules by transfer** — an exit rule's fire RATE is book-specific and a rule that fires on 80% of trades is a new book, not a filter, so any future exit work must report the fire rate before the P&L; (2) **the entry ordinal is the only cross-book regularity this program has found** (6 of 6 on VAL, 3 of 4 on TEST) and it is a frequency cap, which is the opposite of what the owner's throughput needs — it belongs in a pre-registration about WHY later entries lose (slot crowding vs time of day: `D5_r2g` already showed it survives inside the 09:xx hour alone), not in a config; (3) the **ORB universe-level range-size veto** is the only cell in this stage with a dollar mechanism that is measured rather than inferred, and it needs its own pre-registration with `D1_orb`'s slot dose-response as the companion.

## 2026-09-18 — Stage N1 (DATABENTO: order flow at the ORB breakout minute, the CKS plan)
- RAN: `N_databento/PREREG.md` first, then `N1/fetch_tbbo.py` (EQUS.MINI `tbbo`, 09:29:30–09:46:00 ET per date, one request per date for all 13,033 D1 candidate symbol-days; **whole plan priced with `metadata.get_cost` before the first byte — $2.78 against a $150 budget**), `N1/features.py` (`ofi_range`/`tai_range` on 09:30–09:35, `ofi_break60`/`tai_break60`/`spread_at_break_bps` on the 60 s after the first print above `range_high`, `range_high` from the Alpaca 09:30–09:34 bars, cache.db read-only), `N1/cells.py` (the **12 pre-registered cells**, each an 8-slot book through the D1 code path — `study_orb_pipeline_static_lock.py` + `ORB_BT_RESIM_CACHE`, N=8, per_pos_cap $3,333.33, Q1 on — via two hooks in that pipeline that are inert unless their env var is set; **parity control: hooks off reproduces `D1/book_n8_q1on.csv` at 215 picks / $14,428.62**), `N1/perm.py` (200 within-date shuffles of the best cell, full pipeline re-run each time), `N1/mde.py`. Nothing outside `N1/` written; no config, service, cache, cron or order touched; the tbbo parquet is gitignored.
- SAYS: **no causal cell beats the baseline by more than noise.** Best causal cell = a bottom-quintile `ofi_range` veto: TRAIN 0.540 R/pick (t 3.37) vs baseline 0.486 (t 3.21), book **$14,729 vs $14,429 over 21 months — +$300 from removing 9 of 215 picks**, same MDD/worst month/red months; permutation p = 0.060 for that one cell, **≈ 0.5 adjusted for the 12 looked at**. Every other causal cell is worse than doing nothing (the tai_range / tai_break60 / spread vetoes each cut 37–57 picks worth +$2.3K to +$3.5K). The two live-usable break60 hold rules (exit at fill+10 min when the imbalance ≤ 0 and under +0.25R) both lose. **The four `break60`-as-selection cells are non-causal by construction** (the feature is measured after the resting stop-limit has filled) and one of them, `comp ofi_break60`, is the best-looking book in the whole table at **$17,270 (+20%) with the best TEST R** — recorded as a worked example of the failure mode, never as a result. Two data facts outrank the table: EQUS.MINI is a venue subset, so **54.4% of the book's own 215 picks have fewer than 20 prints in the entire opening range** and `ofi_break60` / `spread_at_break_bps` are missing on 48% / 44% of them (PREREG's own "empty on the thin half is not a feature" rule fires), and `tbbo` carries the BBO only **at trades**, so this OFI is a sampled CKS sum, not a message-level one. Availability assertions: 12,451 range + 4,706 break60, zero failures. Price-scale check: 4 of 200 (2.0%) breakout prints outside the Alpaca minute bar, no systematic scale error. PLAN §1's frequency arm (≥ 5 trades/week) and G2's ≥ 55% weeks-green **fail for the baseline itself** (2.6–3.6 picks/week, 38.9% weeks green) — this book cannot clear those gates at any feature.
- NEXT: nothing enabled, nothing proposed, no independent rebuild triggered. **Power is the headline**: the smallest per-pick effect TRAIN could resolve at 80% power is **0.424 R — 87% of the book's entire existing edge** (VAL 1.081 R, TEST 0.379 R), so this null says almost nothing about effects of a plausible size, exactly as PLAN §1's phrasing rule warns. The CKS question is not answered, it is **unanswerable on this population**: re-ask it where the MDE can fall under ~0.1R — the HOD-break book at 25–30 signals/week — and on a consolidated tape (XNAS.ITCH for Nasdaq names) that removes the 54% thin share and gives a true message-level OFI. N2 and N3 are unblocked and unchanged.

## 2026-09-18 — Stage N2 (DATABENTO: 2024H2 daily history → K2 with a real 52-week lookback)
- RAN: `N_databento/PREREG.md` §N2. `N2/fetch_daily_2024h2.py` (EQUS.SUMMARY `ohlcv-1d` ALL_SYMBOLS 2024-07-01→2024-12-31, **priced with `metadata.get_cost` before the first byte — $2.2241 against a $5 stop**; 1,421,466 records / 11,970 symbols / 128 sessions), `N2/convert_daily_2024h2.py` (ALL_SYMBOLS DBN carries no mappings → free `symbology.resolve`, 0 unmapped rows; `bar_date` = UTC date of `ts_event`, the existing file's convention) → `data/research/databento/equs_daily_2024H2.parquet`, **same column layout and dtypes as `equs_daily_2025_2026.parquet`, which was never opened for writing**. Price-scale check vs `data/cache.db::daily_bars` (read-only) on 200 matched keys: OHLC within 0.01% on 99.0%, **1 of 200 (0.5%) off by > 0.5%** — `ALDF 2024-12-31`, a Databento close of exactly 0.0000 (the zero-price pathology `K/step0.md` found; `build_k` NaNs those rows before any rolling window), volume 0% off. Then `N2/build_panel_n2.py` (6,464,692 rows, 16,263 symbols, 548 sessions 2024-07-01..2026-09-04, panel field definitions copied verbatim from `build_daily_panel.py`) and `N2/run_n2.py`, which **imports `K/build_k.py` and `K/report_k.py` unmodified** and patches only PANEL / output dir / SPLITS / FAMILIES-HOLDS (K2, holds 10 and 5, books 10 and 20 = the 4 pre-registered cells), plus a universe AND on `bar_date >= 2025-07-01` (first day the 250-session window is full). Splits TRAIN 2025-07..12 / VAL 2026-01..05 / TEST 2026-06..09. Nothing outside `N2/` and the two gitignored parquets was written; no config, service, cache, cron or order touched.
- SAYS: **the truncated lookback was not what was wrong with K2. `high52` coverage on the liquid universe goes 79.08% → 99.07% (TRAIN) and 0 of 4 cells still pass G1**: TRAIN net −11.6 / −18.4 / −91.5 / −70.4 bps per trade (t −0.11 … −1.76) for h10_n10 / h10_n20 / h5_n10 / h5_n20. G2 is unreachable and **TEST was not read** (`signal_counts.csv`'s TEST column is a count of 526 signals, never a return). The one positive cell is Stage K's same tail: `K2_h10_n20` VAL +154.3 bps (t 1.17, 56.5% weeks green) but TRAIN −18.4, **−164.7 bps ex-top-5%**, +3.0 bps with winners capped, permutation p 0.41 across the 4 cells. Secondary (non-wrapper) control has the same shape (VAL +131.1, t 0.68), so the class map is not hiding an edge. Capacity is again real and unearned: $0.41–0.56M per position → a $4.2–11.1M book. **Power is the honest headline — per-trade SD 900–2,100 bps means the MDE is 80–205 bps/trade on TRAIN and 96–314 on VAL** (TEST would have been ≈1.5× VAL): a half-year TRAIN split at 6–19 trades/week cannot resolve anything a multi-day book plausibly earns. This is a low-power null in THIS universe / horizon / book size / window / cost model, not a refutation of the 52-week-high effect. Cells: 4 here, 24 cumulative for the K family. Data cost $2.22.
- NEXT: nothing enabled, nothing proposed. **The N2 decision rule fires negative: the XNAS.ITCH 2018-05→2024-06 daily pull (≈$23) was NOT priced and NOT pulled** — it was conditional on K2 clearing G1 *and* G2. The 2024H2 parquet is on disk and is now the standard front-extension for any daily-panel study (it also un-truncates K1/K3/K4/K5's rolling fields, should a future pre-registration want them). If the multi-day line is re-opened it needs a window long enough to give a sub-30 bps MDE — i.e. years of TRAIN, which means the XNAS.ITCH history, which means a NEW pre-registration that earns the $23 on a different argument than this one.

## 2026-09-18 — Databento opportunity review (literature + costing only; no data pulled, no engine/config touched)
- RAN: `research/lit_review_2026/DATABENTO_OPPORTUNITIES.md` — 12 strategy families ranked by (published effect size × executability on Alpaca × power of the test our entitlements buy), every dollar figure from `metadata.get_cost` on the live key (21 priced slices, §A of the doc), every citation verified live via Crossref/arXiv (WebSearch budget was exhausted; nothing recalled from memory). Inputs: `LOG.md` all stages, `RESULTS.md`, `cost_curve.md`, `cost_by_outcome.md`, `N1`/`N2`/`N3`, `bf_zero` §6/§6a/§10/§11, `bf_zero2`, `data_sources/alpaca_client.py` order/TIF sites, `data/cache.db::universe` (read-only).
- SAYS: **no candidate has both a strong published tradable effect and clean executability on our stack** — the two channels XNAS.ITCH uniquely opens (`imbalance`, `status`) have the thinnest peer-reviewed *tradable* effect sizes, and the strongly-published ones (ETF arb Box et al. 2021 JFE; CKS order-flow impact) are either unexecutable at 1–3 s / DAY-limit / no-depth-feed or are contemporaneous-explanatory rather than predictive. Four facts decide the ranking: (1) XNAS.ITCH is **11% of consolidated volume** (N3) so every book/trade schema on it is a venue subset — `imbalance` and `status` are the only two exempt, auctions being single-venue by construction and LULD halts market-wide; (2) **Nasdaq is only 49.5% of `universe`** (3,639 of 7,346; NYSE 19.9%, ARCA 16.8%, BATS 10.6%, AMEX 3.2%) so auction/definition studies cover about half our names and none of the wrappers; (3) `TimeInForce.DAY` is the **only** TIF in the repo — **zero `opg`/`cls`/`ioc`/`fok`/`gtc`**, `MarketOrderRequest` imported and never used, `extended_hours` never set, and repo-wide **no LULD/trading-status ingestion or halt handler anywhere** (`halt` matches only the account-level kill flag) — while latency is NOT the constraint (1–3 s after a bar close, sub-second on a tick); (4) the cost contract binds before any signal does (blended 0.135 R/trade, viable `spread/R` ≈ 0.05 vs 0.076–0.463 observed, best honest gross edge ever measured ≈ 0.1R). **Rank 1 is LULD halt-resume**: never tested anywhere in this tree, exactly our universe, `status` ALL_SYMBOLS 2018-05→now = **$54.36**, executable with the order types and reaction latency we already have and no new live data contract, and n ≈ 4–8K events on the 2025–26 splits alone ⇒ **MDE ≈ 0.056–0.079R, the only candidate whose MDE sits below the cost line** — its weakness is disclosed, the US LULD literature is market-quality not tradable-return and the large magnet-effect body is China/Taiwan daily price limits, which do not transfer. Cells: 12 surveyed candidates, 26 pre-declared across the 3 proposed studies, **0 run, 0 splits read**.
- NEXT: nothing enabled, nothing bought. Shortlist in §3, ordered: **S1 halt-resume $66** (12 cells; $0.70 step-0 check that `status` carries actions for NYSE-listed names and that Alpaca's `statuses` websocket channel resolves on our key — without the latter a positive result is unshippable); **S2 MBO/level-1 order flow at the break level $60** (6 cells; N1's own explicitly-named remedy — "the same features on a consolidated tape (XNAS.ITCH for Nasdaq names)" — but it conditions books whose gross edge is +0.003/+0.007R, so a precise zero is the pre-stated likely outcome, and shipping it would need a recurring Databento live MBO subscription); **S3 closing-cross imbalance $240**, contingent on a **$0** gate (paper-trade a 1-share `tif='cls'` LOC; if Alpaca rejects it or fills away from the official Nasdaq print the study never runs) and on the owner carrying a live `imbalance` subscription — prior disclosed: M20, the price-triggered version of that horizon, is dead with gross below cost. Total $366 if all three. Separately recommended regardless of any study: `definition` ALL 2024-07→now = **$35.20**, which retires the test-ticker/survivorship/split class of false positive that cost us F6's TEST (`ZVZZT` = +50.80R of a +39.00R result).

## 2026-09-18 — Stage O / S1 (DATABENTO: LULD halt-resume, the §S1 shortlist's rank-1 study)
- RAN: `lit_review_2026/DATABENTO_OPPORTUNITIES.md` §S1, pre-registration frozen in `O_halt/score_cells.py`'s docstring before any split was read. `step0_cost.py` + `step0_probe.py` (every request priced with `metadata.get_cost` first; enums decoded off the live feed), `fetch_status.py` (XNAS.ITCH `status` ALL_SYMBOLS 2025-01→2026-09-18 **month by month**, filtered to the ET 09:25–16:05 tape of halted instruments inside the loop and never materialised — 5.6M in-session rows → 58K kept rows → two ~200 KB parquets, whole raw stage 122 MB, gitignored), `build_events.py`, `coverage.py`, `score_cells.py`, `ssr.py`, `robustness.py`, `survivors.py`. **$12.47 spent of the approved $100**, and the two unspent lines are the point: the **`definition` schema is not needed at all** — `symbology.resolve(instrument_id→raw_symbol)` is a **FREE metadata endpoint** and resolved **100.0000% of 21,070 events, 0 not_found** (saves the $35.20 on this and every future ALL_SYMBOLS pull), and the **$41.92 2018–2024 deep history was not bought because it cannot be scored** — the 1-min tape on this node starts 2025-01-02, so TRAIN is the PLAN §1 split (2025), not the brief's 2018-05→2024-12. Nothing outside `O_halt/` written; no config, service, cache, cron or order touched; `cache.db`/`bars_sip.db` opened `mode=ro`.
- SAYS: **two facts first.** (1) **Venue: XNAS.ITCH `status` is a PRIMARY-LISTING halt feed — 0.00% of 21,070 LULD pauses are on a NYSE- or BATS-listed name** (NASDAQ 72.4% of the 1,702 halted symbols, 27.4% not in `universe`, ARCA/AMEX 0.06% each), while 20 NYSE megacaps resolve on the feed and carry `PRE_OPEN`/`TRADING`/`SSR_CHANGE` but never a halt action — so the §0(a) "UNVERIFIED" caveat resolves **negatively** and any rule from this source covers the NASDAQ **49.5%** of `universe` and none of the ARCA/BATS wrappers. (2) **Taxonomy**: LULD halt = `PAUSE(9)`+`LULD_PAUSE(50)` (21,072 in-session rows), resume = the next `TRADING(7)` with `is_trading='Y'` (21,070 pairs, median duration exactly 5.0 min, 2 unresumed); news halts are `HALT`+`NEWS_PENDING`/`QUOTING`+`NEWS_AND_RESUMPTION_TIMES` (274) and regulatory `HALT`+`NONE` (3,111) — counted, never mixed. **Result: 2 of 12 pre-declared cells clear G1, G2 and G3** — up-halt/fade/+5m (TRAIN +0.566 t 2.90 / VAL +0.915 t 2.94 77.3% wks / TEST +0.883 t 2.84 87.5% wks) and down-halt/continuation/+5m (+0.476 t 2.10 / +1.107 t 3.10 / +0.943 t 2.64) — and they are ONE rule the cell names hid: both are SHORTS whose 0.6% cap is a *floor*, so both fire when the reopening print comes back **at or above** the pre-halt price (median accepted gap +4.16%). Union book **+0.530 / +0.996 / +0.904 net R at 15.9 / 16.1 / 16.5 trades per week, 20 of 21 months positive, weeks green 76.9 / 90.9 / 100%**. **Then it dies on obtainability**: `is_short_sell_restricted` at the resume (measured, not assumed) is **Y on 55.5% of all LULD events and on 75.6% / 76.8% of the booked trades** — a marketable sell at the next bar's open is exactly what Reg SHO 201 forbids — and on the SSR='N' subset **neither cell clears its own gate** (A fails G2, VAL t 0.24; B fails G1, TRAIN t 1.95). Three more disclosures: TRAIN goes **negative (−0.189) with winners capped at +3R**; the gross edge breaks even at a **2.85% quoted spread** while the out-of-population `cost_curve.md` charges 0.31–0.32% and half the book is $5–10 microcaps minutes after a halt; permutation p across the 12 cells = **0.0535**, i.e. at the threshold, not through it. Robustness: the up-side cell survives open→open on all three splits (not a bid-ask artefact), the down-side one loses ~40% of TRAIN. Cells: **12** this stage, 12 of the 26 pre-declared in §B. Power: MDE 0.414 / 0.656 / 0.666 R on the full book, **1.05 R and 1.25 R on the SSR-free subsets — larger than the effect**, so that null is weak.
- NEXT: nothing enabled, nothing proposed, no engine work. **One step-0 item is deliberately OPEN**: the Alpaca `statuses` websocket check was NOT run — `subscribe_trading_statuses` exists in alpaca-py 0.43.2 and appears **nowhere in the repo** (no halt ingestion of any kind), but Alpaca allows one market-data socket per key and the live `onemil-trader` holds it, so probing it mid-session would have knocked the live stream off; it must run **off-hours or on a separate key**, and without it there is no live rule at any P&L. If the line is re-opened the two cheap decisive measurements come before any new cell: that $0 websocket check, and a `bbo-1s`/`tbbo` pull at the resume minute for these 2,550 events to replace the out-of-population spread constant — either can kill it, and both are cheaper than the $41.92 deep history left unbought. S2 (MBO at the break level) and S3 (closing cross) are unchanged; **the free `symbology.resolve` finding retires the $35.20 `definition` line from all three**.

## 2026-09-18 — Stage N3 (DATABENTO: the published stocks-in-play ORB replayed in ITS period with OUR simulator)
- RAN: `N_databento/PREREG.md` §N3. `N3/fetch_daily.py` (XNAS.ITCH `ohlcv-1d` ALL_SYMBOLS 2018-05-01→2024-01-01, **priced before the first byte — $19.3759 against the $30 stop**) + `N3/fix_symbol_map.py` (ITCH re-issues instrument_ids **alphabetically every day** and its daily `ts_event` is 00:00 UTC of the SESSION date, so the first build's ET conversion joined every bar to the PREVIOUS day's map — NVDA 2023-06-15 came out at $11.42, SPY at $0.87, while AAPL id 32 was correct; caught by the price sanity check, repaired from the free symbology endpoint without re-paying) → `xnas_daily.parquet` 12,284,593 rows / 18,575 symbols. `N3/calibrate_venue_share.py` ($0.2954: XNAS is a **venue** subset, median share **0.118** of consolidated volume on ≥1M-share names, so the paper's 1M ADV filter becomes 118,424 XNAS shares; EQUS.SUMMARY confirmed consolidated against Alpaca SIP). `N3/build_pool.py` (~1,538 eligible names/day) → `N3/fetch_1m.py window` ($11.98, ALL_SYMBOLS 09:30–09:35 per session × 1,288) → `N3/rank_top20.py` (**25,135 picks, median exactly 20/day**, 12,304 long / 12,446 short / 385 doji) → `N3/fetch_1m.py tape` ($5.48, the 20 names 09:30–16:00 × 1,258 sessions) → `N3/sim.py`. Zarattini-Barbon-Aziz rule verbatim (RV = 09:30–09:35 volume ÷ its own prior-14-day mean, top 20, first-5-min candle colour, stop order at the candle's high/low, **R = 0.10 × ATR14**, no target, 16:00 exit). **Total data spend $37.13.** Independent rebuild by a second agent from prose only, forbidden to read `sim.py`: trade sets identical (20,651/20,651), `max |entry diff| = 0`, `max |rr diff| = 1.4e-14`, pooled mean 0.173001 both. Nothing outside `N3/` written; no config, service, cache, cron or order touched.
- SAYS: **the pre-registered criterion splits, and the split IS the result. The simulator passes; the cost contract fails.** Under our own fill and exit machinery at **zero cost** the published book is reproduced in sign and size — **+0.173 R/trade vs a published +0.08R, positive in 5 of 5 years (+0.176/+0.021/+0.213/+0.240/+0.215), +3,573 R total, and a daily portfolio hit ratio of 48.4% against the paper's published 48.4%.** The same trades under **our banded spread table read −0.619 R/trade** (−12,779 R) and under a flat 40 bps −0.435R; at 10 bps the book is **+0.021R, i.e. exactly break-even**. The arithmetic is the whole story: **median R on this book is 0.403% of price**, so one 40 bps round trip **is 0.99 R of cost** and the band table's 50–190 bps is 1.2R–4.7R — a spread measured on $5–20 illiquid movers charged to KR/LEN/HLT/AAPL. PLAN §3 H7, demonstrated on a book nobody here designed. **Third finding, not pre-registered:** the paper's OWN fill (at the level) reads **−0.284R, wrong sign**, and the entire A↔B gap is whether the trigger bar's own low can stop you out — **34.5% of paper-fill trades die inside their own trigger minute** (ours 21.3%) because R ≈ 40 bps is smaller than a first-hour 1-minute bar; the published number is a sub-minute claim 1-minute bars cannot settle. That convention is worth **+0.40R at stops under 1% of price and +0.06 to +0.18R at the 1–3% stops our own books use**. Shape checks: RV buckets are hump-shaped, not monotone (>30× is +0.086R, not the published +0.38R); both sides carry it (long +0.156, short +0.190); the book is its right tail (B0 ex-top-5% **−0.549R**) exactly as the paper describes. Approximations that could move it: the ADV translation is load-bearing (re-ranking at 200K/300K keeps ~20 picks/day but only **60.4%/37.3%** of the same picks), bars are venue bars, ATR from the feed-day file is 5.2% wide, 2019–2023 covers 5 of the paper's 8 years, no borrow cost on the short half.
- NEXT: nothing enabled, nothing proposed, no engine touched. Three carried forward. (1) **"Our simulator loses money on everything" is refuted** — it reproduces a published positive book at zero cost, so the summer nulls are not a fill/exit artefact and our convention is the FAVOURABLE one, not a pessimistic one. (2) **The spread constant is the null and it is now the only unfinished input**: the banded table must never be applied to liquid names, and on the summer population (R ≈ 1–3% of price) it still charges 0.2–0.5R — the same order as every effect those stages tried to detect. The honest next step for `bf_zero2`/`score4` is **Stage B's per-trade measured NBBO**, not another entry family. (3) **Any book with a sub-1% stop cannot be scored on 1-minute bars at all** — the entry-bar convention alone moves it 0.4R; that is a standing rule now, and it retires the idea of tightening stops to raise R-multiples in this program. And the scale to keep in mind: a peer-reviewed 8-year intraday edge is worth ≈ +0.17R gross and ≈ 0.00R at 10 bps of spread.

## 2026-09-18 — Stage J CLOSED; disk reclaim
- Stage J is resolved by its own gates (0/144 cells past G1 on TRAIN); VAL/TEST never open and TEST stays
  unread. U3 tape + candidates_u3.csv deleted (6.6 GB, owner GO); scripts/reports/TRAIN outputs kept.
- Earlier the same day: closed-stage intermediates deleted (bf_zero2, B, C, D, D1, E, G, ignition_*, the
  June trades backup, the April cache backup) — 13 GB total, disk 89% -> 78%. No tracked file removed.
- Owner GO on three asks: keep ORB at 8 slots, reclaim the Stage J disk, buy the point-in-time listings file.

## 2026-09-18 — Stage P (the MEASURED per-trade cost of the honest ORB book, replacing the band constant)
- RAN: `P_cost/` — `exit_times.py` (the exit instants did not exist: an instrumented twin of the SHIPPED `simulate_winner_stack` that also records bar timestamps, **asserting twin == shipped on all 7,402 entered rows — 0 mismatches, reason == dump 100.00%, max |pnl_pct diff| 0.000000, 0 unresolved**), `fetch_spreads.py` (**Alpaca SIP consolidated NBBO**, 7,402 entry minutes quotes+trades + 7,402 exit minutes quotes, 63.5 min), `spreads.py` (rails R1 causality / R2 obtainability / R3 test tickers — all asserted and passed, 0 test tickers), `rescore.py` (5 cost arms x {8,3} slots through the SAME pipeline path, `asis` reproducing D1's `book_n8_q1on` to the cent: 215 picks / $14,428.62), `decompose.py`, `crosscheck_dbn.py`. **Databento spend $0.00 of the $50 allowed** — deliberately: the on-disk N1 `tbbo` is EQUS.MINI, a VENUE subset whose BBO reads median 1.25x and p90 12.7x the NBBO on the 4,568 overlapping breakout prints, and the band table being compared against was itself built from Alpaca SIP, so buying exit-minute `tbbo` would have paid money for a worse and non-comparable number. Nothing outside `P_cost/` written; `cache.db` opened read-only; no config, order, cron or service touched.
- SAYS: **the band constant is 1.92x the measured MEDIAN spread of this book and 1.03x its measured MEAN (n-weighted: band 40.6 bps vs median 21.1 / minute-median 25.1 / mean 39.5 bps) — and it changes no ORB conclusion, because ORB's median R is 4.03% of price so the entire cost question is 0.096 R per pick.** Re-scored 8-slot book, identical 215 picks: zero-cost $16,451 (+0.565R) > **MEASURED $15,221 (+0.520R; TRAIN/VAL/TEST +0.515/+0.854/+0.216)** > as-is 30/10 bps $14,429 (+0.495) > band table $13,635 (+0.469); 3 slots $8,049 / **$7,544** / $7,187 / $6,878. The shipped 30/10 model is **$792 pessimistic** (entry measured $697 cheaper — the NBBO ask was at or inside the stop-limit cap on 73.5% of fills — exit $502 worse). Cross-sectional dispersion inside a band cell is **p90/p10 5.1x-22.2x with only 27-49% of trades within 2x of the constant**, and the band cannot price the **17.6% of fills under $5** at all (its own population was filtered price>=5). The breakout minute's median does NOT understate the fill instant (median ratio **1.000**, mean 1.037, instant wider on only 32% of fills) — the band's error is median-vs-mean and population, never timing. Tails: every arm stays positive ex-top-5% (+0.14 to +0.19R) and t moves only 3.71 -> 4.08 across the whole span of cost models. **The one big number here is not a cost: on 14.4% of fills the NBBO ask at the trigger instant was ABOVE the stop-limit's own cap** (`range_high x 1.003`), which the BT books as a fill; treated as the live no-fill outcome the 8-slot book is $9,239 (+0.332R) and the 3-slot VAL split goes negative — **four times the entire cost question**, and it belongs to the fill model.
- NEXT: **do NOT re-score B/C/E/J** (13-17 compute hours, ~13 GB, on a node with 26 GB free and a live trader): the band table is already IN-population for those stages (it was sampled from `bf_zero2/candidates3.csv`), so this stage's 1.9x gap — which exists because ORB's names are the liquid end of the mover distribution — has no measured basis for transferring; and Stage C states the ceiling itself (net -0.073R vs gross -0.016R = the whole band charge is 0.057R/trade), while its best cell is t 1.93 against a 107-cell permutation null p95 of 3.88 and its closest miss sits inside its own MDE even at zero cost. Three free contract fixes instead, no rebuild needed: charge the cell **MEAN** not the median (`acore.corrected_spread_table`, one line, makes every past number more conservative), extend the table **below $5**, and stop charging a **capped limit** the whole spread distribution — the fills you get are the left tail (73.5% at or inside the cap) and the right tail is non-fills, so the honest contract is the pair (`entry at min(ask, cap)`, `ask > cap => no fill`). The decisive next measurement is the **60-minute quote path** behind the 14.4% (another $0 Alpaca pull, ~1 h) — it is worth ~$6,000 of the 21-month ORB book, six times everything Stage P measured.

## 2026-09-18 — Stage Q (the FILL MODEL behind Stage P's 14.4% flag, settled against the live account)
- RAN: `Q_fill/` — `walk_quotes.py` (Alpaca SIP NBBO walked over the FULL life of all 1,040 flagged orders, trigger print -> the 10:35 `time_stop_canceled` cancel read out of `orb_engine._cancel_stale_pending_orders`; paged, **0 truncated, 0 errors**), `resim_delayed.py` (the 956 later fills re-simulated with the SHIPPED `simulate_winner_stack` through Stage P's instrumented twin; **asserts the 524 same-bar fills reproduce as-is to 1.1e-11**), `rescore_q.py` (8 fill/cap arms x {8,3} slots down the SAME pipeline path, `asis` reproducing D1 to the cent — 215 picks / $14,428.62 / 3 slots $7,186.64), `cap_sweep.py`, `tails.py`, `summarize.py`, and three read-only `trades.db`/`cache.db` probes (`live_crosscheck.py`, `live_lag.py`, `live_trigger_quotes.py`). Databento spend $0.00; nothing outside `Q_fill/` written; no config, order, service or cron touched.
- SAYS: **the flagged orders mostly DO fill — 91.9% of them, median 20.8 s later, at the CAP (they rest as a bid there) — so the truth is neither as-is nor Stage P's hard no-fill. 8 slots: as-is $14,429 (+0.495R) / hard no-fill $8,158 (+0.295) / MEASURED $12,197 (+0.416, cap as the engine sends it to the cent) or $10,297 (+0.344, the BT's unrounded cap); 3 slots $7,187 / $2,243 / $5,044.** Stage P's 14.4% is **10.3%** against the rounded limit the engine actually submits. **82% of the whole correction is SIX picks of 215** (ARQQ, BNAI, ANTX, ATPC, RPGL, CAST) and two of them — including the biggest, ANTX +$1,616 — are not flagged at all once the cap is rounded to the cent. Every arm stays positive on mean R and ex-top-1%; the 3-slot book's t falls 2.71 -> 1.90 (and its VAL goes negative under the hard arm) while the 8-slot book's holds at 3.39 — this question is an argument FOR 8 slots. **Live ground truth (`trades.db`, 166 ORB orders): of the 121 whose stop elected, 116 filled (95.9%) and 5 never did; 19 of the 116 (16.4%) filled in a later minute and 15 of those filled at EXACTLY the cap; all 116 filled at or below the cap, median 13.8 bps below. The trigger-instant ask over `range_high` is within 30 bps on 83.9% of 124 live orders vs 85.6% simulated, within 50 on 94.4% vs 93.9%, within 100 on 99.2% vs 98.4%, median 11.3 vs 11.9 bps — the simulated fill model reproduces the live account's entry microstructure to ~2 points.**
- NEXT: **recommend (do not apply) `orb.yaml::entry.stop_limit_buffer_bps: 30 -> 50`** — measured honestly (the 701 converted orders fill at the ACTUAL ask and are re-simulated from the breakout bar, so the wider cap's cost is charged): 8-slot **$14,048, +0.479R, MDD -576, worst -151, 2 red months**, better on ALL THREE splits than the measured 30-bps book, +$1,851/+$3,751; 100 bps is $376 WORSE (an interior optimum, not a free lunch); the no-chase protection is untouched (QUBX/GLXU/IREX ran 1,168-1,869 bps through the level and stay no-fills), and live agrees — **4 of the 5 live elected-but-never-filled orders had an ask 33-40 bps over `range_high`, inside a 50-bps cap**. Caveats stated in the report: 3 cap values read on the full sample, the gain rests on the same six-pick tail, +0.063R/pick is well inside the book's 0.424R MDE, and queue priority is not modelled (the hard-no-fill arm is that bound). **The $14,429 in circulation should be restated as $12.2K ($581/month at $10K stage); nothing about the live 8-slot book needs to stop.** Still outstanding from P: `acore.corrected_spread_table()` takes `.median()` where it owes the MEAN — that one-liner is untouched by this stage (arms (a)/(b)/(c) charge no band constant) and remains to be done.
